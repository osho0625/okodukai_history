# 差分設計書（詳細） - skills/fs-design-phase8-object/object-designer-prompt.md

対象ファイル: `skills/fs-design-phase8-object/object-designer-prompt.md`

## 変更対象3-1: クラス設計の共通要件（全モード共通）への「技術的実装情報」テンプレート新設

approach.mdの記載の通り、本ファイルにはこれまで「技術的実装情報」テンプレート自体が存在していなかったため、既存5カテゴリ＋新規2カテゴリをまとめて新設する。これは既存構造の変更ではなく新規セクションの追加であり、リファクタリングには該当しない（approach.md リファクタリング検討結果を参照）。

### before
```markdown
## クラス設計の共通要件（全モード共通）

各クラスについて以下を明確に定義すること:

### 役割（Responsibility）
- そのクラスが「何のために」存在するか（1文で表現できること）

### パブリックメソッド
- メソッド名、引数（型・制約・デフォルト値）、戻り値（型）、処理概要
- 副作用の有無
- 送出する例外（Raises）の一覧と条件

### パブリックプロパティ
- 型、読み取り専用かどうか、制約条件

### 依存関係
- コンストラクタで受け取る依存（DI対象）
- 依存先はインターフェース（抽象）とする

---

## オブジェクトの生成・管理パターン（全モード共通）
```

### after
```markdown
## クラス設計の共通要件（全モード共通）

各クラスについて以下を明確に定義すること:

### 役割（Responsibility）
- そのクラスが「何のために」存在するか（1文で表現できること）

### パブリックメソッド
- メソッド名、引数（型・制約・デフォルト値）、戻り値（型）、処理概要
- 副作用の有無
- 送出する例外（Raises）の一覧と条件

### パブリックプロパティ
- 型、読み取り専用かどうか、制約条件

### 依存関係
- コンストラクタで受け取る依存（DI対象）
- 依存先はインターフェース（抽象）とする

### 技術的実装情報（該当する場合）

| カテゴリ | 内容 |
|---|---|
| API呼び出し手順 | {該当する場合: エンドポイント、認証方式、リクエスト/レスポンス形式} |
| データ変換ルール | {該当する場合: 入力→出力の変換ロジック、マッピングルール} |
| エラーハンドリング方針 | {該当する場合: 想定例外、リトライ方針、フォールバック} |
| 外部ライブラリ使用方法 | {該当する場合: ライブラリ名、使用するAPI、バージョン制約} |
| 設定値・定数 | {該当する場合: 設定ファイル参照先、環境変数、デフォルト値} |
| 技術調査結果 | {該当する場合: 外部ツール・外部サービス連携部分に関する tech-investigation の調査結果要約} |
| 参考ドキュメント（URLリンク付き） | {該当する場合: 参照した公式ドキュメントへのURLリンク（最低1件、複数可）} |

※該当するカテゴリのみ記載する。該当なしの場合はセクション自体を省略してよい。

---

## オブジェクトの生成・管理パターン（全モード共通）
```

### 変更理由
REQ-C-001（AC-001, AC-002）対応。approach.mdの方針「本ファイルにはこれまで当該テンプレートが存在しなかったため、既存5カテゴリ＋新規2カテゴリをまとめて新設する」を反映する。`skills/object-design/object-designer-prompt.md` の reverse モードテンプレート（変更対象2-4）と同一の7カテゴリ構成・同一の運用ルール（該当するカテゴリのみ記載）を採用し、共通スキル側とローカル複製側の記述を一貫させる。

---

## 変更対象3-2: mode: phase8_{layer-name} 処理手順（技術調査ステップの追加）

### before
```markdown
### 処理手順

1. `layered-architecture.md` から対象レイヤー（{layer_name}）の責務定義・依存ルールを読み込む
2. 対象レイヤーの責務に応じて、設計すべきクラス群を判断する:
   - アプリケーション層相当（ユースケース実行、DTO変換、DI組み立て）→ ユースケースクラス、DTO、例外クラス、DI方針、設定クラスを設計
   - インフラ層相当（外部リソースアクセス、リポジトリ実装）→ リポジトリ具象クラス、外部アダプタ、テスト用ダミー実装、データマッピングを設計
   - プレゼンテーション層相当（UI制御、画面遷移、イベントハンドリング）→ 画面クラス、UIイベント対応、ダイアログ、スレッド管理、エントリーポイントを設計
   - その他のレイヤー → layered-architecture.md の責務定義に従い、適切なクラス設計を判断する
3. 各クラスについて共通要件（役割、パブリックメソッド、パブリックプロパティ、依存関係）を定義する
4. SOLID原則・テスタビリティの確保を適用する
5. `{output_file}` に成果物を Write で作成する
6. ユーザーに提示し合意を得る
```

### after
```markdown
### 処理手順

1. `layered-architecture.md` から対象レイヤー（{layer_name}）の責務定義・依存ルールを読み込む
2. 対象レイヤーの責務に応じて、設計すべきクラス群を判断する:
   - アプリケーション層相当（ユースケース実行、DTO変換、DI組み立て）→ ユースケースクラス、DTO、例外クラス、DI方針、設定クラスを設計
   - インフラ層相当（外部リソースアクセス、リポジトリ実装）→ リポジトリ具象クラス、外部アダプタ、テスト用ダミー実装、データマッピングを設計
   - プレゼンテーション層相当（UI制御、画面遷移、イベントハンドリング）→ 画面クラス、UIイベント対応、ダイアログ、スレッド管理、エントリーポイントを設計
   - その他のレイヤー → layered-architecture.md の責務定義に従い、適切なクラス設計を判断する
3. 各クラスについて共通要件（役割、パブリックメソッド、パブリックプロパティ、依存関係）を定義する
4. SOLID原則・テスタビリティの確保を適用する
5. 対象クラスが外部ツール・外部サービスと連携する場合、tech-investigation (aide-powers skill) を実施し、調査結果の要約を「技術調査結果」カテゴリ、参照した公式ドキュメントのURLを「参考ドキュメント（URLリンク付き）」カテゴリとして、技術的実装情報セクションに反映する
6. `{output_file}` に成果物を Write で作成する
7. ユーザーに提示し合意を得る
```

### 変更理由
REQ-C-002（AC-003, AC-004）対応。approach.mdの方針「実際に新規クラス設計が行われる fs-design-phase8-object/object-designer-prompt.md（mode: phase8_{layer-name}）の処理手順に、技術調査ステップを挿入する」を反映する。既存Write手順（旧5）の直前に新規ステップを挿入し、後続ステップの番号を1つずつ後ろにずらす（旧5→6、旧6→7）。既存ステップの意味・順序関係自体は変更しない。

---

## 変更対象3-3: mode: fix 処理手順 手順1

### before
```markdown
### 処理手順

1. QA指摘内容を読み込み、問題の種別を特定する:
   - SOLID原則違反 → 該当する原則に基づいてクラス設計を修正する
   - レイヤー間依存違反 → 依存方向を修正し、インターフェースを導入する
   - テスト容易性の問題 → DI可能な構造に修正する
   - オブジェクト定義の品質問題 → 役割・メソッド・プロパティの定義を改善する
```

### after
```markdown
### 処理手順

1. QA指摘内容を読み込み、問題の種別を特定する:
   - SOLID原則違反 → 該当する原則に基づいてクラス設計を修正する
   - レイヤー間依存違反 → 依存方向を修正し、インターフェースを導入する
   - テスト容易性の問題 → DI可能な構造に修正する
   - オブジェクト定義の品質問題 → 役割・メソッド・プロパティの定義を改善する
   - 外部連携部分の技術調査・参考ドキュメント不足 → tech-investigation (aide-powers skill) を実施し、調査結果と参考ドキュメントリンクを技術的実装情報セクションに反映する
```

### 変更理由
REQ-C-003対応（object-design-qa-agent の新設検証項目Lに対する修正手段の整備）。approach.mdの方針「問題種別の箇条書きに『外部連携部分の技術調査・参考ドキュメント不足』を追加する」を反映する。既存4項目の箇条書きに1項目追加するのみで、既存の問題種別・対応方法は変更しない。
