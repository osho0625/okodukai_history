# 差分設計書（詳細） - skills/object-design/object-designer-prompt.md

対象ファイル: `skills/object-design/object-designer-prompt.md`

## 変更対象2-1: quality_check モード 品質基準チェック項目（カテゴリ9の新設、カテゴリ数表記の更新）

### before（カテゴリ数表記）
```markdown
以下の8カテゴリを全レイヤーに適用して検証する。
```

### after（カテゴリ数表記）
```markdown
以下の9カテゴリを全レイヤーに適用して検証する。
```

### before（カテゴリ9の新設位置）
```markdown
#### 8. 技術的実装情報の反映チェック（全レイヤー）
- system-requirements.md に記載された技術情報のうち、該当クラスに関連するものが「技術的実装情報」セクションに反映されているか
- 技術調査結果（tech-investigation）で判明した情報が該当クラスに反映されているか
- 該当する技術情報がある場合に「技術的実装情報」セクションが存在するか（該当なしの場合はセクション省略可）

### 出力
```

### after（カテゴリ9の新設位置）
```markdown
#### 8. 技術的実装情報の反映チェック（全レイヤー）
- system-requirements.md に記載された技術情報のうち、該当クラスに関連するものが「技術的実装情報」セクションに反映されているか
- 技術調査結果（tech-investigation）で判明した情報が該当クラスに反映されているか
- 該当する技術情報がある場合に「技術的実装情報」セクションが存在するか（該当なしの場合はセクション省略可）

#### 9. 外部連携部分の技術調査・参考ドキュメントチェック（全レイヤー）
- 外部ツール・外部サービスと連携するクラスについて、「技術的実装情報」セクションに「技術調査結果」カテゴリが記載されているか
- 同セクションに「参考ドキュメント（URLリンク付き）」カテゴリが記載され、最低1件のURL形式のリンクが含まれているか
- 記載された調査結果・リンクの内容が、当該クラスの他の設計記述（API呼び出し手順等）と整合しているか
- 外部連携が存在しないクラスは本チェックの対象外とする

### 出力
```

### 変更理由
REQ-C-001（AC-001, AC-002）・REQ-C-002（AC-003）対応。approach.mdの方針「既存テーブルへの行追加として統合」を反映し、既存カテゴリ8の直後にカテゴリ9を新設する。既存の8カテゴリの記述・順序は変更しない。あわせて、カテゴリ新設により検証対象カテゴリ数が8から9に増えるため、直前の総数表記「以下の8カテゴリを全レイヤーに適用して検証する。」を「以下の9カテゴリを全レイヤーに適用して検証する。」に更新する（QA指摘によるFAIL対応、既存記述との数値不整合を解消）。

---

## 変更対象2-2: delta モード 処理手順 手順7

### before
```markdown
7. 技術情報の反映を確認する:
   - system-requirements.md、技術調査結果（存在する場合）から、追加・変更するクラスに関連する技術情報を特定する
   - 該当する技術情報を「技術的実装情報」セクションに反映する
8. before→after 形式の差分設計結果を {changes_dir}/delta-object-design.md に Write で作成する
9. ユーザーに提示し合意を得る
```

### after
```markdown
7. 技術情報の反映を確認する:
   - system-requirements.md、技術調査結果（存在する場合）から、追加・変更するクラスに関連する技術情報を特定する
   - 該当する技術情報を「技術的実装情報」セクションに反映する
   - 変更が外部ツール・外部サービス連携部分に及ぶ場合、tech-investigation (aide-powers skill) を実施し、調査結果の要約を「技術調査結果」カテゴリ、参照した公式ドキュメントのURLを「参考ドキュメント（URLリンク付き）」カテゴリにそれぞれ反映する
8. before→after 形式の差分設計結果を {changes_dir}/delta-object-design.md に Write で作成する
9. ユーザーに提示し合意を得る
```

### 変更理由
REQ-C-002（AC-003, AC-004）対応。approach.mdの方針「delta モードでは既存レイヤーへの変更が外部連携部分に及ぶ場合のみ tech-investigation を実施する」を手順7の箇条書きに追加する。既存の手順番号（7〜9）は変更せず、手順7内の箇条書きに1項目追加するのみ。

---

## 変更対象2-3: reverse モード 処理手順 手順10

### before
```markdown
10. 技術的実装情報を抽出する:
    - コードから読み取れるAPI呼び出し手順、データ変換ルール、エラーハンドリング方針、外部ライブラリ使用方法、設定値・定数を特定する
    - system-requirements.md（存在する場合）の技術情報と突合する
    - 該当する情報がある場合に「技術的実装情報」セクションに記載する
11. レイヤー別ファイルを生成する:
```

### after
```markdown
10. 技術的実装情報を抽出する:
    - コードから読み取れるAPI呼び出し手順、データ変換ルール、エラーハンドリング方針、外部ライブラリ使用方法、設定値・定数を特定する
    - system-requirements.md（存在する場合）の技術情報と突合する
    - 該当する情報がある場合に「技術的実装情報」セクションに記載する
    - 外部ツール・外部サービスと連携するクラスについては、既存コードのコメントやREADME等から参考ドキュメントURLを抽出できる場合は「参考ドキュメント（URLリンク付き）」カテゴリに記録する。抽出できない場合は tech-investigation (aide-powers skill) で補足調査してもよい。調査結果の要約は「技術調査結果」カテゴリに記載する
11. レイヤー別ファイルを生成する:
```

### 変更理由
REQ-C-001・REQ-C-002（AC-003）対応。approach.mdの方針「reverse モードでは既存コードのコメントやREADME等から参考ドキュメントURLを抽出できる場合は記録し、抽出できない場合は tech-investigation で補足調査してもよいという任意規定とする」を手順10の箇条書きに追加する。既存の手順番号は変更しない。

---

## 変更対象2-4: reverse モード 出力形式テンプレート（技術的実装情報テーブル）

### before
```markdown
#### 技術的実装情報

| カテゴリ | 内容 |
|---|---|
| API呼び出し手順 | {該当する場合: エンドポイント、認証方式、リクエスト/レスポンス形式} |
| データ変換ルール | {該当する場合: 入力→出力の変換ロジック、マッピングルール} |
| エラーハンドリング方針 | {該当する場合: 想定例外、リトライ方針、フォールバック} |
| 外部ライブラリ使用方法 | {該当する場合: ライブラリ名、使用するAPI、バージョン制約} |
| 設定値・定数 | {該当する場合: 設定ファイル参照先、環境変数、デフォルト値} |

※該当するカテゴリのみ記載する。該当なしの場合はセクション自体を省略してよい。
```

### after
```markdown
#### 技術的実装情報

| カテゴリ | 内容 |
|---|---|
| API呼び出し手順 | {該当する場合: エンドポイント、認証方式、リクエスト/レスポンス形式} |
| データ変換ルール | {該当する場合: 入力→出力の変換ロジック、マッピングルール} |
| エラーハンドリング方針 | {該当する場合: 想定例外、リトライ方針、フォールバック} |
| 外部ライブラリ使用方法 | {該当する場合: ライブラリ名、使用するAPI、バージョン制約} |
| 設定値・定数 | {該当する場合: 設定ファイル参照先、環境変数、デフォルト値} |
| 技術調査結果 | {該当する場合: 外部ツール・外部サービス連携部分に関する tech-investigation の調査結果要約} |
| 参考ドキュメント（URLリンク付き） | {該当する場合: 参照した公式ドキュメントへのURLリンク（最低1件、複数可）} |

※該当するカテゴリのみ記載する。該当なしの場合はセクション自体を省略してよい。
```

### 変更理由
REQ-C-001（AC-001, AC-002）対応。approach.mdの方針「既存の『技術的実装情報』テーブルに新規カテゴリ行を2行追加する」を、reverse モードの出力テンプレートに反映する。既存5カテゴリの行・列構成・注記（※該当するカテゴリのみ記載する）は変更せず、末尾に2行追加する。1セル内にMarkdownリンクを複数列挙できる構造のため、AC-002（最低1件のURLを記載できる構造）を満たす。
