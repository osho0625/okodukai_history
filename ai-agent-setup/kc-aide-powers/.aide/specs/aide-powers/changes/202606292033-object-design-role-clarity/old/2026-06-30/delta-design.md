# 差分設計書

## 設計方針
- 対応方針: 追加で対応（OCP準拠）。既存のスキル定義・エージェント定義・プロンプトテンプレートの構造を変更せず、セクション追加・ルール追加・検証項目追加のみで実現する。
- 設計系共通スキル結果: なし（局所的変更）

---

## 修正対象の差分設計

### 変更対象1: skills/object-design/SKILL.md — 品質基準セクションへのルール追加

#### before

```markdown
## 品質基準（全モード共通）

### SOLID原則の適用基準

| 原則 | チェック観点 |
|---|---|
| S（単一責任） | クラスの変更理由が1つに限定されているか。bool型引数で他引数の役割が変わるメソッドがないか |
| O（オープン・クローズド） | 拡張ポイント（インターフェース、抽象クラス）が適切に設けられているか。新ユースケース追加時に既存クラスの修正が不要か |
| L（リスコフ置換） | 基底型で派生型を扱えるか。契約（事前条件・事後条件）が守られているか |
| I（インターフェース分離） | クライアントが使わないメソッドへの依存を強制していないか |
| D（依存性逆転） | 上位モジュールが下位モジュールの具象に依存していないか。DIによる依存注入が適切に設計されているか |

### テスタビリティの確保

| レイヤー | テスタビリティ基準 |
|---|---|
| ドメイン層 | モック不要でテスト可能な純粋ロジック |
| アプリケーション層 | DIでダミー実装を注入してテスト可能 |
| インフラ層 | テスト用ダミー実装（dry run）を必ず設計に含める |
| プレゼンテーション層 | ロジックをアプリケーション層に委譲し、UI層は薄く保つ |
```

#### after

```markdown
## 品質基準（全モード共通）

### SOLID原則の適用基準

| 原則 | チェック観点 |
|---|---|
| S（単一責任） | クラスの変更理由が1つに限定されているか。bool型引数で他引数の役割が変わるメソッドがないか |
| O（オープン・クローズド） | 拡張ポイント（インターフェース、抽象クラス）が適切に設けられているか。新ユースケース追加時に既存クラスの修正が不要か |
| L（リスコフ置換） | 基底型で派生型を扱えるか。契約（事前条件・事後条件）が守られているか |
| I（インターフェース分離） | クライアントが使わないメソッドへの依存を強制していないか |
| D（依存性逆転） | 上位モジュールが下位モジュールの具象に依存していないか。DIによる依存注入が適切に設計されているか |

### テスタビリティの確保

| レイヤー | テスタビリティ基準 |
|---|---|
| ドメイン層 | モック不要でテスト可能な純粋ロジック |
| アプリケーション層 | DIでダミー実装を注入してテスト可能 |
| インフラ層 | テスト用ダミー実装（dry run）を必ず設計に含める |
| プレゼンテーション層 | ロジックをアプリケーション層に委譲し、UI層は薄く保つ |

### 役割定義→publicメソッド導出ルール

クラスの役割定義から必要なpublicメソッドを網羅的に導出するためのルール。全モード・全レイヤーに適用する。

**導出手順:**
1. クラスの「役割定義（具体）」セクション（入力・出力・責務外）を確認する
2. 「入力」で宣言されたデータを受け取るためのメソッドを特定する
3. 「出力」で宣言されたデータを返すためのメソッドを特定する
4. 入力→出力の変換に必要な中間処理のメソッドを特定する
5. 全ての責務に対応するpublicメソッドが存在することをセルフチェックする

**セルフチェック基準:**
- 役割定義で宣言した全ての「入力」に対応する受信経路（メソッド引数またはコンストラクタ引数）が存在すること
- 役割定義で宣言した全ての「出力」に対応する生成経路（メソッド戻り値）が存在すること
- 「責務外」に記載された処理に関するメソッドが含まれていないこと

**トレーサビリティ:**
- メソッドテーブルの「責務」列に、対応する役割定義の項目（入力/出力のどれに対応するか）を明記する
```

#### 変更理由

既存の品質基準6カテゴリ（SOLID/テスタビリティ/貧血症/依存違反/ダミー実装/ユビキタス言語）に「役割定義→publicメソッド導出ルール」を7つ目として追加する。これにより、クラスの役割定義で宣言した責務に対応するpublicメソッドの漏れを設計段階で防止する。既存カテゴリの内容・構造は一切変更しない。

---

### 変更対象2: skills/object-design/object-designer-prompt.md — reverse モード出力形式のクラス一覧テンプレートへのセクション追加

#### before

```markdown
### {クラス名}

| 項目 | 内容 |
|---|---|
| 役割 | {クラスの責務} |
| 継承 | {基底クラス / なし} |
| 実装IF | {実装するインターフェース / なし} |

#### メソッド

| メソッド名 | 引数 | 戻り値 | 責務 |
|---|---|---|---|
| {method_name} | {args} | {return_type} | {description} |

#### プロパティ

| プロパティ名 | 型 | 読取専用 | 説明 |
|---|---|---|---|
| {prop_name} | {type} | {yes/no} | {description} |

#### 依存関係

| 依存先 | 注入方法 | 用途 |
|---|---|---|
| {dependency} | コンストラクタ | {purpose} |

#### テスト観点
- {テスト観点1}
- {テスト観点2}

※現状の課題（該当する場合のみ）:
- {SOLID違反やテスタビリティの問題点}
```

#### after

```markdown
### {クラス名}

| 項目 | 内容 |
|---|---|
| 役割 | {クラスの責務} |
| 継承 | {基底クラス / なし} |
| 実装IF | {実装するインターフェース / なし} |

#### 役割定義（具体）

| 観点 | 内容 |
|---|---|
| 入力（何を受け取るか） | {このクラスが受け取るデータ・イベント・依存を具体的に列挙} |
| 出力（何を返すか） | {このクラスが生成・返却するデータ・副作用を具体的に列挙} |
| 責務外（何を担当しないか） | {このクラスが明示的に担当しない処理を列挙} |

#### メソッド

| メソッド名 | 引数 | 戻り値 | 責務 | 対応する役割定義 |
|---|---|---|---|---|
| {method_name} | {args} | {return_type} | {description} | {入力:○○ / 出力:○○} |

#### プロパティ

| プロパティ名 | 型 | 読取専用 | 説明 |
|---|---|---|---|
| {prop_name} | {type} | {yes/no} | {description} |

#### 依存関係

| 依存先 | 注入方法 | 用途 |
|---|---|---|
| {dependency} | コンストラクタ | {purpose} |

#### 技術的実装情報

| カテゴリ | 内容 |
|---|---|
| API呼び出し手順 | {該当する場合: エンドポイント、認証方式、リクエスト/レスポンス形式} |
| データ変換ルール | {該当する場合: 入力→出力の変換ロジック、マッピングルール} |
| エラーハンドリング方針 | {該当する場合: 想定例外、リトライ方針、フォールバック} |
| 外部ライブラリ使用方法 | {該当する場合: ライブラリ名、使用するAPI、バージョン制約} |
| 設定値・定数 | {該当する場合: 設定ファイル参照先、環境変数、デフォルト値} |

※該当するカテゴリのみ記載する。該当なしの場合はセクション自体を省略してよい。

#### テスト観点
- {テスト観点1}
- {テスト観点2}

※現状の課題（該当する場合のみ）:
- {SOLID違反やテスタビリティの問題点}
```

#### 変更理由

1. 「役割定義（具体）」セクション追加（REQ-C-001）: 既存の「役割」フィールド（1行の概要）はそのまま残し、その直後に入力・出力・責務外の3項目を明記する「役割定義（具体）」セクションを追加する。これにより、クラスの責務が具体的に定義され、曖昧さが排除される。
2. メソッドテーブルに「対応する役割定義」列追加（REQ-C-002）: 各publicメソッドがどの責務に対応するかのトレーサビリティを確保する。既存の4列（メソッド名/引数/戻り値/責務）は変更せず、5列目として追加する。
3. 「技術的実装情報」セクション追加（REQ-C-003）: 技術調査等で判明した実装に必要な技術情報を該当クラスの設計に漏れなく記載する場所を確保する。テスト観点の直前に配置する。

---

### 変更対象3: skills/object-design/object-designer-prompt.md — delta モード処理手順へのステップ追加

#### before

```markdown
### 処理手順

1. impact-analysis.md を読み込み、オブジェクト設計に影響するレイヤーを特定する
2. approach.md を読み込み、変更方針を把握する
3. 影響を受けるレイヤーの object-design-*.md を Read で読み込む（参照のみ、変更しない）
4. 変更要求に基づき、以下の観点で差分を設計する:
   - クラスの追加・削除・変更
   - メソッドの追加・削除・シグネチャ変更
   - 依存関係の変更
   - インターフェースの変更
5. SOLID原則・テスタビリティを維持しながら差分を設計する
6. before→after 形式の差分設計結果を {changes_dir}/delta-object-design.md に Write で作成する
7. ユーザーに提示し合意を得る
```

#### after

```markdown
### 処理手順

1. impact-analysis.md を読み込み、オブジェクト設計に影響するレイヤーを特定する
2. approach.md を読み込み、変更方針を把握する
3. 影響を受けるレイヤーの object-design-*.md を Read で読み込む（参照のみ、変更しない）
4. 変更要求に基づき、以下の観点で差分を設計する:
   - クラスの追加・削除・変更
   - メソッドの追加・削除・シグネチャ変更
   - 依存関係の変更
   - インターフェースの変更
5. SOLID原則・テスタビリティを維持しながら差分を設計する
6. 役割定義→publicメソッド導出セルフチェックを実施する:
   - 追加・変更するクラスの「役割定義（具体）」で宣言した全責務に対応するpublicメソッドが存在するか確認する
   - 欠落がある場合はメソッドを追加する
7. 技術情報の反映を確認する:
   - system-requirements.md、技術調査結果（存在する場合）から、追加・変更するクラスに関連する技術情報を特定する
   - 該当する技術情報を「技術的実装情報」セクションに反映する
8. before→after 形式の差分設計結果を {changes_dir}/delta-object-design.md に Write で作成する
9. ユーザーに提示し合意を得る
```

#### 変更理由

deltaモードの処理手順にステップ6（役割定義→publicメソッド導出セルフチェック）とステップ7（技術情報反映確認）を追加する。これにより、差分設計時にもメソッド漏れの防止と技術情報の反映が担保される。既存のステップ1〜5は変更せず、ステップ5の後に新ステップを挿入し、旧ステップ6〜7を8〜9に繰り下げる。

---

### 変更対象4: skills/object-design/object-designer-prompt.md — quality_check モード品質基準チェック項目への追加

#### before

```markdown
### 品質基準チェック項目

以下の6カテゴリを全レイヤーに適用して検証する。

#### 1. SOLID原則チェック（全レイヤー）
（省略: 既存内容変更なし）

#### 2. テスタビリティチェック（全レイヤー）
（省略: 既存内容変更なし）

#### 3. ドメインモデル貧血症チェック（ドメイン層）
（省略: 既存内容変更なし）

#### 4. レイヤー間依存違反チェック
（省略: 既存内容変更なし）

#### 5. ユビキタス言語整合性チェック
（省略: 既存内容変更なし）

#### 6. ダミー実装設計チェック（インフラ層）
（省略: 既存内容変更なし）
```

#### after

```markdown
### 品質基準チェック項目

以下の8カテゴリを全レイヤーに適用して検証する。

#### 1. SOLID原則チェック（全レイヤー）
（既存内容変更なし）

#### 2. テスタビリティチェック（全レイヤー）
（既存内容変更なし）

#### 3. ドメインモデル貧血症チェック（ドメイン層）
（既存内容変更なし）

#### 4. レイヤー間依存違反チェック
（既存内容変更なし）

#### 5. ユビキタス言語整合性チェック
（既存内容変更なし）

#### 6. ダミー実装設計チェック（インフラ層）
（既存内容変更なし）

#### 7. 役割定義→publicメソッド網羅性チェック（全レイヤー）
- 各クラスに「役割定義（具体）」セクションが存在するか
- 「入力」で宣言されたデータに対応する受信経路（メソッド引数 or コンストラクタ引数）が存在するか
- 「出力」で宣言されたデータに対応する生成経路（メソッド戻り値）が存在するか
- メソッドテーブルの「対応する役割定義」列が全メソッドに記載されているか
- 「責務外」に記載された処理に関するメソッドが含まれていないか

#### 8. 技術的実装情報の反映チェック（全レイヤー）
- system-requirements.md に記載された技術情報のうち、該当クラスに関連するものが「技術的実装情報」セクションに反映されているか
- 技術調査結果（tech-investigation）で判明した情報が該当クラスに反映されているか
- 該当する技術情報がある場合に「技術的実装情報」セクションが存在するか（該当なしの場合はセクション省略可）
```

#### 変更理由

quality_checkモードの品質基準チェック項目を6カテゴリから8カテゴリに拡張する。カテゴリ7（役割定義→publicメソッド網羅性）とカテゴリ8（技術的実装情報の反映）を追加する。既存の6カテゴリの内容は一切変更しない。

---

### 変更対象5: skills/object-design/object-designer-prompt.md — reverse モード処理手順へのステップ追加

#### before

```markdown
### 処理手順

1. program-structure.md を読み込み、プロジェクトのファイル構成を把握する
2. layered-architecture.md が存在する場合、レイヤー分類の基準を把握する
3. 全クラスを抽出する:
   - クラス名、継承関係、実装インターフェース
   - パブリックメソッド（名前、引数、戻り値）
   - パブリックプロパティ（型、読み取り専用かどうか）
   - コンストラクタの依存注入パターン
4. 依存関係を解析する:
   - import文の解析
   - コンストラクタインジェクションの解析
   - クラス間の参照関係
5. レイヤー分類を行う:
   - layered-architecture.md の定義に基づく分類
   - 存在しない場合はディレクトリ構成とクラスの特性から推定
6. 例外クラスを抽出する
7. デザインパターンを識別する（リポジトリ、ファクトリ、アダプタ、オブザーバー等）
8. テスト観点を抽出する（テストファイルが存在する場合）
9. レイヤー別ファイルを生成する:
   - object-design-domain.md
   - object-design-application.md
   - object-design-infrastructure.md
   - object-design-presentation.md
10. object-design.md（概要）を生成する:
    - 設計方針のサマリ
    - 全レイヤーのクラス一覧
    - レイヤー間のクラス関連図（Mermaid記法）
11. ユーザーに提示し合意を得る
```

#### after

```markdown
### 処理手順

1. program-structure.md を読み込み、プロジェクトのファイル構成を把握する
2. layered-architecture.md が存在する場合、レイヤー分類の基準を把握する
3. 全クラスを抽出する:
   - クラス名、継承関係、実装インターフェース
   - パブリックメソッド（名前、引数、戻り値）
   - パブリックプロパティ（型、読み取り専用かどうか）
   - コンストラクタの依存注入パターン
4. 依存関係を解析する:
   - import文の解析
   - コンストラクタインジェクションの解析
   - クラス間の参照関係
5. レイヤー分類を行う:
   - layered-architecture.md の定義に基づく分類
   - 存在しない場合はディレクトリ構成とクラスの特性から推定
6. 例外クラスを抽出する
7. デザインパターンを識別する（リポジトリ、ファクトリ、アダプタ、オブザーバー等）
8. テスト観点を抽出する（テストファイルが存在する場合）
9. 各クラスの役割定義（具体）を導出する:
   - コードの実際の振る舞いから「入力（何を受け取るか）」「出力（何を返すか）」「責務外（何を担当しないか）」を特定する
   - メソッドテーブルの各メソッドに「対応する役割定義」列を付与する
10. 技術的実装情報を抽出する:
    - コードから読み取れるAPI呼び出し手順、データ変換ルール、エラーハンドリング方針、外部ライブラリ使用方法、設定値・定数を特定する
    - system-requirements.md（存在する場合）の技術情報と突合する
    - 該当する情報がある場合に「技術的実装情報」セクションに記載する
11. レイヤー別ファイルを生成する:
    - object-design-domain.md
    - object-design-application.md
    - object-design-infrastructure.md
    - object-design-presentation.md
12. object-design.md（概要）を生成する:
    - 設計方針のサマリ
    - 全レイヤーのクラス一覧
    - レイヤー間のクラス関連図（Mermaid記法）
13. ユーザーに提示し合意を得る
```

#### 変更理由

reverseモードの処理手順にステップ9（役割定義の導出）とステップ10（技術的実装情報の抽出）を追加する。逆引き時にも新テンプレートの情報を正確に抽出・記載するために必要。既存のステップ1〜8は変更せず、ステップ8の後に新ステップを挿入し、旧ステップ9〜11を11〜13に繰り下げる。

---

### 変更対象6: agents/object-design-qa-agent.md — 検証項目J・Kの追加

#### before

```markdown
#### I. 例外・ルール回避の検出

- 設計原則を「不要」「適用対象外」「適用を限定」等の表現で無効化している記述がないか
- モック禁止・ダミー実装禁止のルールに対して回避策を設計に組み込んでいる記述がないか
- 技術的制約を理由にルールを緩和・撤廃している記述がないか
- 「〇〇は本プロジェクトでは適用しない」「〇〇は簡略化する」等、ルールに対する例外を設計書内で自己宣言している記述がないか
- レイヤードアーキテクチャの依存ルールを「特定のケースでは許容する」等の表現で緩和している記述がないか
- 該当する記述を発見した場合はFAIL
```

#### after

```markdown
#### I. 例外・ルール回避の検出

- 設計原則を「不要」「適用対象外」「適用を限定」等の表現で無効化している記述がないか
- モック禁止・ダミー実装禁止のルールに対して回避策を設計に組み込んでいる記述がないか
- 技術的制約を理由にルールを緩和・撤廃している記述がないか
- 「〇〇は本プロジェクトでは適用しない」「〇〇は簡略化する」等、ルールに対する例外を設計書内で自己宣言している記述がないか
- レイヤードアーキテクチャの依存ルールを「特定のケースでは許容する」等の表現で緩和している記述がないか
- 該当する記述を発見した場合はFAIL

#### J. 役割↔publicメソッド網羅性検証

- 各クラスに「役割定義（具体）」セクション（入力・出力・責務外の3項目）が存在するか。存在しなければFAIL
- 「入力」で宣言されたデータ・イベントに対応する受信経路（publicメソッド引数 or コンストラクタ引数）が全て存在するか。欠落があればFAIL
- 「出力」で宣言されたデータ・副作用に対応する生成経路（publicメソッド戻り値 or 副作用を持つメソッド）が全て存在するか。欠落があればFAIL
- メソッドテーブルの「対応する役割定義」列が全publicメソッドに記載されているか。未記載があればFAIL
- 「責務外」に記載された処理を実行するメソッドが含まれていないか。含まれていればFAIL
- 本検証項目は全レイヤー（ドメイン・アプリケーション・インフラ・プレゼンテーション）に適用する

#### K. 技術情報記載有無検証

- 前提成果物（system-requirements.md、layered-architecture.md、技術調査結果）に記載された技術情報のうち、該当クラスに関連するものを特定する
- 該当する技術情報がある場合、そのクラスの「技術的実装情報」セクションに反映されているか。未反映があればFAIL
- 技術情報が反映されるべきクラスに「技術的実装情報」セクション自体が存在しない場合はFAIL
- 技術情報に該当しないクラス（純粋なドメインロジックのみ等）はセクション省略を許容する（FAILとしない）
- 本検証項目は全レイヤー（ドメイン・アプリケーション・インフラ・プレゼンテーション）に適用する
```

#### 変更理由

既存の検証項目A〜Iの後に、新規検証項目J（役割↔publicメソッド網羅性）と検証項目K（技術情報記載有無）を追加する（REQ-C-004、REQ-C-005）。既存の検証項目A〜Iの内容・判定ロジックは一切変更しない。判定基準（FAIL=0かつWARNING=0→APPROVED）も変更不要（新項目もFAIL/PASS判定を使うため既存の判定フレームワークにそのまま載る）。

**ステップ2（考慮漏れ検証）との重複回避について:**
- ステップ2は「要件・技術調査結果の内容が設計記述箇所に記載されているか」を一般的・機能横断的に検証する
- 検証項目Jは「役割定義（具体）セクションの3項目とpublicメソッドの1対1対応」という構造的整合性を検証する（ステップ2にはこの構造的検証はない）
- 検証項目Kは「技術的実装情報セクションへの反映」という特定セクションの存在・内容を検証する（ステップ2は「設計記述箇所」全般を対象としており、特定セクション形式の有無は問わない）

---

### 変更対象7: agents/object-design-qa-agent.md — ステップ番号の調整

#### before

```markdown
### ステップ3: 判定と結果の出力

全検証項目の結果を集約し、総合判定を決定する。出力フォーマットに従って結果を返す。
```

（注: agents/object-design-qa-agent.md（Claude Code版）ではステップ3が「検証項目の実行」であり、その直後に「ステップ3: 判定と結果の出力」と記載されている。実質的にステップ3の後半として記載されているため、番号の変更は不要。既存のままとする。）

#### after

（変更なし — 既存のステップ番号構造をそのまま維持する）

#### 変更理由

検証項目J・Kの追加はステップ3「検証項目の実行」内への追加であり、ステップの番号付けに変更は不要。

---

### 変更対象8: agents/kiro/object-design-qa-agent.md — 検証項目J・Kの追加（Kiro IDE版）

変更対象6と同一の差分を `agents/kiro/object-design-qa-agent.md` にも適用する。

#### before

（変更対象6のbeforeと同一 — agents/kiro/ 版は agents/ ルート版と同一内容）

#### after

（変更対象6のafterと同一）

#### 変更理由

Kiro IDE版のエージェント定義にも同等の検証項目を追加し、プラットフォーム間の整合性を維持する。

---

### 変更対象9: agents/kiro/prompts/object-design-qa-agent-prompt.md — 検証項目J・Kの追加（Kiro CLI版）

Kiro CLIのプロンプトファイルにも変更対象6と同一の検証項目J・Kを追加する。

#### before

（変更対象6のbeforeと同一の検証項目I部分 — agents/kiro/prompts/ 版）

#### after

（変更対象6のafterと同一 — 検証項目I の後に J・K を追加）

#### 変更理由

Kiro CLI版のプロンプトにも同等の検証項目を追加し、プラットフォーム間の整合性を維持する。

---

### 変更対象10: agents/kiro/object-design-qa-agent.json — description更新

#### before

```json
{
  "name": "object-design-qa-agent",
  "description": "オブジェクト設計QAレビューエージェント。ドメイン層・アプリケーション層・インフラ層・プレゼンテーション層のオブジェクト設計書の品質を検証する。",
  "prompt": "file://./prompts/object-design-qa-agent-prompt.md",
  "tools": ["@builtin"],
  "allowedTools": ["@builtin"]
}
```

#### after

```json
{
  "name": "object-design-qa-agent",
  "description": "オブジェクト設計QAレビューエージェント。ドメイン層・アプリケーション層・インフラ層・プレゼンテーション層のオブジェクト設計書の品質を検証する。役割↔publicメソッド網羅性、技術情報記載有無を検証する。",
  "prompt": "file://./prompts/object-design-qa-agent-prompt.md",
  "tools": ["@builtin"],
  "allowedTools": ["@builtin"]
}
```

#### 変更理由

新検証項目J・Kの追加に伴い、descriptionフィールドに「役割↔publicメソッド網羅性、技術情報記載有無を検証する。」を追記する。JSON構造（name, prompt, tools, allowedTools等）は変更しない。beforeを実ファイルの実際の内容に合わせて修正した。

---

## 新規追加の設計
なし（既存ファイルへのセクション追加のみ）

## インターフェース影響サマリ
- シグネチャ変更: なし
- 後方互換性: あり（既存の入力・出力・呼び出しインターフェースは全て維持される）
- 呼び出し元への影響: なし（object-design スキルの Input from caller は変更なし。QAエージェントの入力定義も変更なし。テンプレートの拡張は出力側の充実であり、呼び出し元のインターフェースに影響しない）

## 更新が必要な設計資料
- `agents/kiro/object-design-qa-agent.md` — 検証項目J・K追加（本差分設計に含む）
- `agents/kiro/prompts/object-design-qa-agent-prompt.md` — 検証項目J・K追加（本差分設計に含む）
- `agents/kiro/object-design-qa-agent.json` — description更新（本差分設計に含む）
