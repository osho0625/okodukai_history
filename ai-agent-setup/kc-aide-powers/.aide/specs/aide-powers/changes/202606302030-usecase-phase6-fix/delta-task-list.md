# 差分タスクリスト

## 概要

| 項目 | 値 |
|---|---|
| 変更名 | usecase-phase6-fix |
| 親タスク数 | 7（D-001〜D-006 + D-R-001） |
| 成果物種別 | 非プログラム（Markdown） |
| テスト方式 | 手動確認（dev-environment.md §7.4） |
| 対象ディレクトリ | `skills/fs-design-phase6-usecase/`（グローバル: `$env:USERPROFILE\.kiro\skills\fs-design-phase6-usecase\`） |

---

## 依存関係図

```
D-001 (usecase-lister-prompt.md)          ──┐
D-002 (usecase-process-analyzer-prompt.md) ──┤
D-003 (usecase-coverage-reviewer-prompt.md)──┼──→ D-005 (SKILL.md)
D-004 (usecase-removal-prompt.md 新規)     ──┤         │
                                             │         ↓
                                             └──→ D-006 (program-structure.md)
                                                       │
                                                       ↓
                                              D-R-001 (リグレッション確認)
```

**依存の根拠:**
- D-005 は D-001〜D-004 に依存: SKILL.md が参照するステータス値（DONE/FAIL等）および削除プロンプトが、各プロンプトファイルに定義されている必要があるため
- D-006 は D-004 に依存: 新規ファイル作成完了後にプログラム構成書を更新するため
- D-R-001 は D-005 に依存: SKILL.md の変更完了後にリグレッション確認を行うため

---

## タスク一覧

### D-001: usecase-lister-prompt.md の変更

| 項目 | 値 |
|---|---|
| 対象ファイル | `skills/fs-design-phase6-usecase/usecase-lister-prompt.md` |
| 変更種別 | 変更（末尾追記） |
| テストファイル | N/A |
| 依存先 | なし |
| delta-design対応 | 変更対象6 |

#### 変更内容
- ファイル末尾（「## 注意事項」セクションの後）に「## 報告フォーマット」セクションを追加
- Status定義: DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED / FAIL の5種と各発生条件
- 追加報告項目: 作成したUCの総数、利用者の分類数、懸念事項

#### 動作確認観点
- 追加した「## 報告フォーマット」セクションがファイル末尾に正しく配置されていること
- Status の5種の定義が delta-design.md 変更対象6の after と一致すること
- 既存の「## 注意事項」セクションの内容が変更されていないこと

---

### D-002: usecase-process-analyzer-prompt.md の変更

| 項目 | 値 |
|---|---|
| 対象ファイル | `skills/fs-design-phase6-usecase/usecase-process-analyzer-prompt.md` |
| 変更種別 | 変更（末尾追記） |
| テストファイル | N/A |
| 依存先 | なし |
| delta-design対応 | 変更対象7 |

#### 変更内容
- ファイル末尾（「## fix モードの場合」セクションの後）に以下2セクションを追加:
  1. 「## プログラム実現不可UCの判定基準」— 5カテゴリ（物理的操作/人間の介在必須/外部ハードウェア制御/法的手続き/対面コミュニケーション）のテーブル + 判定時の注意事項
  2. 「## 報告フォーマット」— Status定義5種 + DONE_WITH_CONCERNS時の「### プログラム実現不可UC一覧」テーブル形式

#### 動作確認観点
- 「## プログラム実現不可UCの判定基準」のカテゴリテーブルが5行あること
- 判定時の注意事項（APIやWeb連携は実現不可としない等）が記載されていること
- 報告フォーマットの「### プログラム実現不可UC報告」テーブルが UC-ID / ユースケース名 / 不可カテゴリ / 理由 の4列であること
- 既存セクション（「## fix モードの場合」以前）の内容が変更されていないこと

---

### D-003: usecase-coverage-reviewer-prompt.md の変更

| 項目 | 値 |
|---|---|
| 対象ファイル | `skills/fs-design-phase6-usecase/usecase-coverage-reviewer-prompt.md` |
| 変更種別 | 変更（既存セクション置換） |
| テストファイル | N/A |
| 依存先 | なし |
| delta-design対応 | 変更対象8 |

#### 変更内容
- 既存「## 報告フォーマット」セクションを拡充版に置換
- Status定義を DONE のみから 5種（DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED / FAIL）に拡充
- 「### 判定」セクションの「結果:」行の値が SKILL.md Step3 の期待値と文字列レベルで一致する旨を明記
- 判定結果の文字列: 「全操作カバー済み」または「未カバー操作あり（{N}件）」

#### 動作確認観点
- 「## 報告フォーマット」セクションが delta-design.md 変更対象8の after と一致すること
- 「**重要:**」注記で文字列一致が明示されていること
- 既存セクション（「## 報告フォーマット」以外）の内容が変更されていないこと

---

### D-004: usecase-removal-prompt.md の新規作成

| 項目 | 値 |
|---|---|
| 対象ファイル | `skills/fs-design-phase6-usecase/usecase-removal-prompt.md` |
| 変更種別 | 新規作成 |
| テストファイル | N/A |
| 依存先 | なし |
| delta-design対応 | 新規追加 |

#### 変更内容
- delta-design.md「新規追加の設計」セクションに定義された全体構造で新規ファイルを作成
- 主要セクション構成:
  - `# ユースケース削除 プロンプトテンプレート`
  - `## あなたの役割`
  - `## feature_name`（プレースホルダー）
  - `## 削除対象`（removal_targets テーブル形式）
  - `## 入力ファイル`
  - `## 実行手順`（3ステップ: UC行削除 → ファイル削除 → 削除結果確認）
  - `## 注意事項`（4項目）
  - `## 報告フォーマット`（Status 5種 + 報告項目）

#### 動作確認観点
- ファイルが `skills/fs-design-phase6-usecase/usecase-removal-prompt.md` に作成されていること
- delta-design.md の新規追加セクションの内容と完全一致すること
- プレースホルダー（{feature_name}, {removal_targets}）が正しく配置されていること
- 報告フォーマットの Status 5種が定義されていること

---


### D-005: SKILL.md の変更

| 項目 | 値 |
|---|---|
| 対象ファイル | `skills/fs-design-phase6-usecase/SKILL.md` |
| 変更種別 | 変更（複数箇所） |
| テストファイル | N/A |
| 依存先 | D-001, D-002, D-003, D-004 |
| delta-design対応 | 変更対象1, 2, 3, 4, 5, 9 |

#### D-005-1: Step2 完了条件修正（変更対象1）

**変更内容:**
- Step2「### 完了条件」から「UCリストのユーザー合意結果(Step2)が『合意』であり、」を削除

**動作確認観点:**
- 完了条件がサブエージェントのステータス（DONE / DONE_WITH_CONCERNS）とファイル存在のみになっていること
- 「ユーザー合意」への言及がStep2完了条件から完全に除去されていること

#### D-005-2: Step2 成果物セクション修正（変更対象2）

**変更内容:**
- Step2 成果物セクションから以下2行を削除:
  - 「・サブエージェントがユーザーと直接対話して得たUCリストの合意結果を、次の項目で記載する」
  - 「　UCリストのユーザー合意結果(Step2):」

**動作確認観点:**
- Step2 成果物セクションに「ユーザー合意結果」への言及が残っていないこと
- 他の成果物項目（サブエージェント出力記載等）が損なわれていないこと

#### D-005-3: Step2 状態判定 FAIL分岐追加（変更対象3）

**変更内容:**
- Step2「### 状態判定」を delta-design.md 変更対象3の after に置換
- FAIL分岐追加: 「ステータスが FAIL の場合 → UCリスト作成に回復不能なエラーが発生している。エラー内容をユーザーに報告し、対応方針（再実行 / 入力情報の修正 / 前フェーズへの差し戻し）を確認する」
- 「ユーザー合意が得られていない場合」分岐を削除

**動作確認観点:**
- FAIL分岐が追加されていること
- 「ユーザー合意が得られていない場合」分岐が削除されていること
- DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED の既存分岐が維持されていること

#### D-005-4: Step3 転記参照箇所明示（変更対象4）

**変更内容:**
- Step3 成果物セクションの記載を delta-design.md 変更対象4の after に置換
- サブエージェント出力「### 判定」→「結果:」行 → 「網羅性レビュー結果(Step3):」への転記明示
- サブエージェント出力「### 未カバーユースケース一覧」→ 「未カバー操作一覧(Step3):」への転記明示

**動作確認観点:**
- 「・サブエージェント出力の「### 判定」セクション内「結果:」行の値を以下の項目に転記する」という記述が存在すること
- 「・サブエージェント出力の「### 未カバーユースケース一覧」テーブル内容を以下に転記する」という記述が存在すること
- 未カバー0件の場合「なし」と記載する指示があること

#### D-005-5: Step5 状態判定 FAIL分岐+プログラム実現不可UCサブフロー追加（変更対象5）

**変更内容:**
- Step5「### 状態判定」を delta-design.md 変更対象5の after に置換
- DONE_WITH_CONCERNS 分岐を2種に分離:
  - 「プログラム実現不可UC」が報告されている場合 → サブフロー（抽出→ユーザー提示→削除承認→usecase-removal-prompt.md 起動）
  - 「プログラム実現不可UC」が含まれない場合 → 従来通り懸念報告
- FAIL分岐追加
- 削除承認の番号付き選択肢（4択）を記載

**動作確認観点:**
- プログラム実現不可UCサブフローの4ステップ（抽出→提示→承認→削除エージェント起動）が記載されていること
- 番号付き選択肢（1. 全て削除を承認 / 2. 一部のみ削除 / 3. 削除しない / 4. その他）が記載されていること
- FAIL分岐が追加されていること（対応方針: 当該UCのみ再実行 / 入力情報の修正 / スキップして次Stepへ）
- 既存の NEEDS_CONTEXT / BLOCKED 分岐が維持されていること
- 「UC削除エージェントの出力(Step5):」としてレポート記載する旨が記述されていること

#### D-005-6: Integration プロンプトテンプレートリスト更新（変更対象9）

**変更内容:**
- SKILL.md の Integration セクション内「**プロンプトテンプレート（本スキルディレクトリ配下）:**」リストに以下1行を追加:
  - `- \`usecase-removal-prompt.md\` — Step 5 プログラム実現不可UC削除サブフロー【新規作成】`

**動作確認観点:**
- 追加行がリスト末尾に存在すること
- 「【新規作成】」タグが付いていること
- 既存の6行のプロンプトテンプレートリストが変更されていないこと

---

### D-006: program-structure.md の更新

| 項目 | 値 |
|---|---|
| 対象ファイル | `.aide/specs/aide-powers/program-structure.md` |
| 変更種別 | 変更（リスト追記） |
| テストファイル | N/A |
| 依存先 | D-004 |
| delta-design対応 | 「更新が必要な設計資料」セクション |

#### 変更内容
- program-structure.md 内の `skills/fs-design-phase6-usecase/` に関連するプロンプトテンプレートの記載箇所に `usecase-removal-prompt.md` を追記

#### 動作確認観点
- `usecase-removal-prompt.md` が program-structure.md 内に追記されていること
- 既存のプロンプトテンプレート一覧が変更されていないこと

---

### D-R-001: リグレッションテスト — Step2 正常フロー確認

| 項目 | 値 |
|---|---|
| 対象 | SKILL.md Step2〜Step5 の正常フロー |
| テスト種別 | リグレッション確認（手動） |
| 依存先 | D-005 |
| impact-analysis対応 | §5.2 R-1〜R-9 |

#### 確認内容

以下の9項目を、変更後の SKILL.md を読み込んで整合性を確認する:

| # | 確認項目 | 確認方法 |
|---|---|---|
| R-1 | Step2 正常フロー: DONE → Step3遷移 | Step2完了条件・状態判定を読み、DONEでStep3遷移が維持されていることを確認 |
| R-2 | Step3 正常フロー: 「全操作カバー済み」→ Step4遷移 | Step3状態判定の判定ロジックが不変であることを確認 |
| R-3 | Step3 未カバー時: fix→再レビューループ動作 | Step3の未カバー時分岐が維持されていることを確認 |
| R-4 | Step4 正常フロー: ユーザー承認→Step5遷移 | Step4が今回変更対象外であることを確認 |
| R-5 | Step5 正常フロー: 全UC DONE → Step6遷移 | Step5完了条件・状態判定を読み、全DONEでStep6遷移が維持されていることを確認 |
| R-6 | Step5 DONE_WITH_CONCERNS（不可能UC以外）: 懸念報告→Step6 | 条件分岐の「プログラム実現不可UCが含まれない場合」パスが従来動作を維持していることを確認 |
| R-7 | Step6以降のフロー: 変更なし | Step6〜Step9の定義が今回変更されていないことを確認 |
| R-8 | Phase 5→Phase 6 遷移: インターフェース不変 | SKILL.md の前処理/mode受付部分が変更されていないことを確認 |
| R-9 | Phase 6→Phase 7 遷移: インターフェース不変 | SKILL.md の後処理部分が変更されていないことを確認 |

#### 判定基準
- 全9項目で「維持されている」と確認できればPASS
- いずれかの項目で既存動作の破壊が検出されればFAIL（修正が必要）

---

## 実装順序（推奨）

1. **Wave 1**（並列実行可能）: D-001, D-002, D-003, D-004
2. **Wave 2**（Wave 1 完了後）: D-005
3. **Wave 3**（D-004 完了後）: D-006
4. **Wave 4**（D-005 完了後）: D-R-001

※ Wave 2 の D-005 は D-001〜D-004 の全完了が前提条件
※ Wave 3 の D-006 は D-004 のみに依存するが、Wave 2 と同時実行も可
