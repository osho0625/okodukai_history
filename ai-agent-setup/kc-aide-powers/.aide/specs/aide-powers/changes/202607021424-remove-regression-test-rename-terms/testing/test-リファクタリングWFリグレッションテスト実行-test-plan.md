# 動作確認試験書：リファクタリングWF リグレッションテスト実行＋開始前基準比較（機能4）

対象機能: リファクタリングWF Step2（fs-refactoring-phase5-impl）
関連タスク: B-004, B-008

## 試験項目1: regression-test-prompt.md に委譲先の固定指定が残っていないことの確認

- **状態、条件**: `skills/fs-refactoring-phase5-impl/regression-test-prompt.md` が最新の修正後の状態であること
- **確認項目**: ユーザーが、リファクタリングWFのStep2で使われるプロンプトファイルを開き、`micro-impl-agent` への固定委譲記述が無いことを目視確認する
- **確認手順**:
  1. `skills/fs-refactoring-phase5-impl/regression-test-prompt.md` をエディタで開く
  2. ファイル内を `micro-impl-agent` という文字列で検索する
  3. 検索結果が0件であることを確認する
  4. 「## 委譲先エージェント」という見出しがファイル内に存在しないことを目視確認する
- **確認環境**: 開発環境
- **期待結果**: 「micro-impl-agent」の検索結果が0件であること。「## 委譲先エージェント」の見出しも存在しないこと
- **確認結果**:
- **試験実施日時**:

## 試験項目2: 開始前基準比較機能（他3ファイルにない独自機能）が維持されていることの確認

- **状態、条件**: 同上ファイルが最新の修正後の状態であること
- **確認項目**: ユーザーが、ファイルを通読し、「## プレースホルダー」独自セクションが削除されている一方で、`{{safety_net_baseline}}` 変数、「### 開始前基準（比較対象）」セクション、基準比較の判定ロジック（完全一致／FAIL数増加／スキップ数変化）、「## 出力」内の「開始前基準との比較結果」項目が全て維持されていることを確認する
- **確認手順**:
  1. `skills/fs-refactoring-phase5-impl/regression-test-prompt.md` を先頭から末尾まで通読する
  2. 「## プレースホルダー（FSが実データで置き替える）」の見出しが存在しないことを確認する
  3. 「### 開始前基準（比較対象）」セクションが「### 開発環境情報」の後に存在し、`{{safety_net_baseline}}` の記載があることを確認する
  4. 「## テスト実行ルール」内に基準値との比較ロジック（完全一致・FAIL数増加・スキップ数の不自然な変化）が記載されていることを確認する
  5. 「## 出力」セクションに「開始前基準との比較結果」の項目が残っていることを確認する
  6. 全体構造がコードブロックに入れ子にされていないことを確認する
- **確認環境**: 開発環境
- **期待結果**: 冒頭見出し「# リグレッションテスト実行エージェント（リファクタリングWF用）」から「## 出力」まで、コードブロックなしの一続きの構造として目視でき、他3ファイルにはない基準比較機能（`{{safety_net_baseline}}`・開始前基準セクション・判定ロジック・比較結果出力項目）がそのまま残っていること
- **確認結果**:
- **試験実施日時**:

## 試験項目3: SKILL.md のIntegration節・Step2本文で委譲先が固定されていないことの確認

- **状態、条件**: `skills/fs-refactoring-phase5-impl/SKILL.md` が最新の修正後の状態であること
- **確認項目**: ユーザーが、SKILL.mdのIntegration節とStep2本文を見て、委譲先が特定のエージェント名で固定されていないことを確認する
- **確認手順**:
  1. `skills/fs-refactoring-phase5-impl/SKILL.md` をエディタで開き、「# Integration」セクションまでスクロールする
  2. 「**呼び出すサブエージェント（Step 2）:**」という見出しが存在することを確認する
  3. その直下に委譲先を固定しない旨の説明文（開始前基準との比較報告を含む旨）があることを確認する
  4. 「サブエージェントプロンプト」欄の `regression-test-prompt.md` の説明が「汎用のサブエージェント用」になっていること、かつ「phase1-statusのセーフティネット基準との比較報告を含む」の記載が維持されていることを確認する
  5. 「## Step 2」本文中に `micro-impl-agent` という具体名が存在せず、代わりに「サブエージェントを起動し」という表現が使われていることを確認する
- **確認環境**: 開発環境
- **期待結果**: 「呼び出すサブエージェント（Step 2）」の見出しの下に委譲先固定なしの説明文が表示され、「サブエージェントプロンプト」欄も「汎用のサブエージェント用」と表示され、Step2本文には「サブエージェントを起動し」という表現のみが使われ `micro-impl-agent` という具体名が表示されないこと
- **確認結果**:
- **試験実施日時**:

## 試験項目4: 他3ファイルのStep本文の表現との統一性、および他Step・coding-test-2review経由の記述が変更されていないことの確認

- **状態、条件**: 同上SKILL.mdが最新の修正後の状態であること。B-005〜B-007修正後の他3ファイルのSKILL.mdも参照可能であること
- **確認項目**: ユーザーが、リファクタリングWFのStep2本文の表現が、他3ファイル（バグ修正WF・変更WF・実装WF）のStep本文で使われている「サブエージェントを起動し」という表現と揺れなく一致していること、また「呼び出す名前付きエージェント（すべて coding-test-2review 経由・Step 1）」欄、Step3（動作確認試験）工程②のmanual-test-review-agent記述が変更されていないことを確認する
- **確認手順**:
  1. `skills/fs-refactoring-phase5-impl/SKILL.md` のStep2本文の「サブエージェントを起動し」という表現を、他3ファイル（`skills/fs-bugfix-phase2-impl/SKILL.md` Step9工程①、`skills/fs-change-phase2-impl/SKILL.md` Step11工程①、`skills/fs-impl-phase4-execution/SKILL.md` Step2工程①）の同等の記述と比較し、表現が一致していることを確認する
  2. 「呼び出す名前付きエージェント（すべて coding-test-2review 経由・Step 1）」欄に `micro-impl-agent (aide-powers agent)` の行がそのまま存在することを確認する
  3. 「呼び出す名前付きエージェント（Step 3 工程②）」欄に `manual-test-review-agent (aide-powers agent)` がそのまま存在することを確認する
- **確認環境**: 開発環境
- **期待結果**: リファクタリングWFのStep2本文の表現が他3ファイルのStep本文の表現と一致し、coding-test-2review経由（Step1）欄・Step3工程②のmanual-test-review-agent記述は変更されずそのまま表示されること
- **確認結果**:
- **試験実施日時**:
