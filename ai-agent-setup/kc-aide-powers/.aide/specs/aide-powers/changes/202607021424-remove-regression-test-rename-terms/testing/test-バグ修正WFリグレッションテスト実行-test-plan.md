# 動作確認試験書：バグ修正WF リグレッションテスト実行（機能1）

対象機能: バグ修正WF リグレッションテスト実行（fs-bugfix-phase2-impl Step9 工程①）
関連タスク: B-001, B-005

## 試験項目1: regression-test-prompt.md に委譲先の固定指定が残っていないことの確認

- **状態、条件**: `skills/fs-bugfix-phase2-impl/regression-test-prompt.md` が最新の修正後の状態であること
- **確認項目**: ユーザー（オーケストレータ運用者）が、バグ修正WFの動作確認Step工程①で使われるプロンプトファイルを開き、実装専任エージェント名（`micro-impl-agent`）に処理を固定委譲する記述が無いことを目視確認する
- **確認手順**:
  1. `skills/fs-bugfix-phase2-impl/regression-test-prompt.md` をエディタで開く
  2. ファイル内を `micro-impl-agent` という文字列で検索する
  3. 検索結果が0件であることを確認する
  4. 「## 委譲先エージェント」という見出しがファイル内に存在しないことを目視確認する
- **確認環境**: 開発環境（リポジトリのファイルを直接開いて確認）
- **期待結果**: 「micro-impl-agent」という文字列が検索結果として1件も表示されないこと。「## 委譲先エージェント」の見出しも存在しないこと
- **確認結果**:
- **試験実施日時**:

## 試験項目2: プレースホルダー独自セクションが削除され、プロンプト本文がトップレベル構造に統一されていることの確認

- **状態、条件**: 同上ファイルが最新の修正後の状態であること
- **確認項目**: ユーザーが、ファイル冒頭から末尾まで目視で通読し、「委譲先エージェント」「プレースホルダー（FSが実データで置き替える）」という独自セクションが無く、説明文の直後から「### タスク情報」が始まり、コードブロック（```）で囲まれていないことを確認する
- **確認手順**:
  1. `skills/fs-bugfix-phase2-impl/regression-test-prompt.md` を先頭から末尾まで通読する
  2. 「## プレースホルダー（FSが実データで置き替える）」という見出しが存在しないことを確認する
  3. 「### タスク情報」〜「## 報告フォーマット」の各見出しが、コードブロック記号（```）に囲まれずファイルの通常のMarkdown構造として並んでいることを確認する
  4. 「## 出力」セクションが末尾に存在することを確認する
- **確認環境**: 開発環境
- **期待結果**: 冒頭見出し「# リグレッションテスト実行エージェント（バグ修正WF用）」→説明文→「### タスク情報」→「### 実行モード」→「### テスト実行コマンド（必須）」→「### 開発環境情報」→「## テスト実行ルール」→「## 報告フォーマット」→「## 出力」が、コードブロックに入れ子にならず一続きの構造として目視できること
- **確認結果**:
- **試験実施日時**:

## 試験項目3: SKILL.md のIntegration節で委譲先が固定されていないことの確認

- **状態、条件**: `skills/fs-bugfix-phase2-impl/SKILL.md` が最新の修正後の状態であること
- **確認項目**: ユーザーが、SKILL.mdのIntegration節を見て、Step9工程①の委譲先が特定のエージェント名で固定されていないことを確認する
- **確認手順**:
  1. `skills/fs-bugfix-phase2-impl/SKILL.md` をエディタで開き、「# Integration」セクションまでスクロールする
  2. 「**呼び出すサブエージェント（Step 9 工程①）:**」という見出しが存在することを確認する
  3. その直下に「委譲先は具体的なエージェント名で固定しない」という趣旨の説明文があることを確認する
  4. 「プロンプトテンプレート」欄の `regression-test-prompt.md` の説明に「micro-impl-agent 用」ではなく「汎用のサブエージェント用」と記載されていることを確認する
- **確認環境**: 開発環境
- **期待結果**: 「呼び出すサブエージェント（Step 9 工程①）」の見出しの下に委譲先固定なしの説明文が表示され、「プロンプトテンプレート」欄も「汎用のサブエージェント用」と表示されること
- **確認結果**:
- **試験実施日時**:

## 試験項目4: coding-test-2review経由の正当なmicro-impl-agent利用が変更されていないことの確認

- **状態、条件**: 同上SKILL.mdが最新の修正後の状態であること
- **確認項目**: ユーザーが、Step8（タスク実装ループ）に関連する「呼び出す名前付きエージェント（すべて coding-test-2review 経由・Step 8）」欄を見て、`micro-impl-agent (aide-powers agent)` の記載が変更されずそのまま残っていることを確認する
- **確認手順**:
  1. `skills/fs-bugfix-phase2-impl/SKILL.md` の「# Integration」セクション内で「呼び出す名前付きエージェント（すべて coding-test-2review 経由・Step 8）」という見出しを探す
  2. その直下に `micro-impl-agent (aide-powers agent)` の行が存在することを確認する
  3. Step9本文（工程①〜④の記述）を通読し、工程②〜④（動作確認試験、manual-test-review-agent呼び出し等）の記述が本修正前と変わっていないことを確認する
- **確認環境**: 開発環境
- **期待結果**: coding-test-2review経由（Step8）欄には `micro-impl-agent (aide-powers agent)` がそのまま表示され、Step9工程②〜④の記述は本修正の影響を受けずそのまま表示されること
- **確認結果**:
- **試験実施日時**:
