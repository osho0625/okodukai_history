# 動作確認試験書：変更WF リグレッションテスト実行（機能2）

対象機能: 変更WF リグレッションテスト実行（fs-change-phase2-impl Step11 工程①）
関連タスク: B-002, B-006

## 試験項目1: regression-test-prompt.md に委譲先の固定指定が残っていないことの確認

- **状態、条件**: `skills/fs-change-phase2-impl/regression-test-prompt.md` が最新の修正後の状態であること
- **確認項目**: ユーザーが、変更WFの動作確認Step工程①で使われるプロンプトファイルを開き、`micro-impl-agent` への固定委譲記述が無いことを目視確認する
- **確認手順**:
  1. `skills/fs-change-phase2-impl/regression-test-prompt.md` をエディタで開く
  2. ファイル内を `micro-impl-agent` という文字列で検索する
  3. 検索結果が0件であることを確認する
  4. 「## 委譲先エージェント」という見出しがファイル内に存在しないことを目視確認する
- **確認環境**: 開発環境
- **期待結果**: 「micro-impl-agent」の検索結果が0件であること。「## 委譲先エージェント」の見出しも存在しないこと
- **確認結果**:
- **試験実施日時**:

## 試験項目2: プロンプト本文（変更WF固有の変数含む）がトップレベル構造に統一されていることの確認

- **状態、条件**: 同上ファイルが最新の修正後の状態であること
- **確認項目**: ユーザーが、ファイルを通読し、「## プレースホルダー」独自セクションが無く、`{{changes_dir}}` を含むプロンプト本文がコードブロックに入れ子にされずトップレベル構造として存在することを確認する
- **確認手順**:
  1. `skills/fs-change-phase2-impl/regression-test-prompt.md` を先頭から末尾まで通読する
  2. 「## プレースホルダー（FSが実データで置き替える）」の見出しが存在しないことを確認する
  3. 「呼び出し元ワークフロー: 変更WF（fs-change-phase2-impl Step11）」の記述が「### タスク情報」内にそのまま存在することを確認する
  4. 全体構造がコードブロックに入れ子にされていないことを確認する
- **確認環境**: 開発環境
- **期待結果**: 冒頭見出し「# リグレッションテスト実行エージェント（変更WF用）」から「## 出力」まで、コードブロックなしの一続きの構造として目視でき、変更WF固有の記述（`{{changes_dir}}`、Step11の言及）が変更前と同じ内容で残っていること
- **確認結果**:
- **試験実施日時**:

## 試験項目3: SKILL.md のIntegration節で委譲先が固定されていないことの確認

- **状態、条件**: `skills/fs-change-phase2-impl/SKILL.md` が最新の修正後の状態であること
- **確認項目**: ユーザーが、SKILL.mdのIntegration節を見て、Step11工程①の委譲先が特定のエージェント名で固定されていないことを確認する
- **確認手順**:
  1. `skills/fs-change-phase2-impl/SKILL.md` をエディタで開き、「# Integration」セクションまでスクロールする
  2. 「**呼び出すサブエージェント（Step 11 工程①）:**」という見出しが存在することを確認する
  3. その直下に委譲先を固定しない旨の説明文があることを確認する
  4. 「プロンプトテンプレート」欄の `regression-test-prompt.md` の説明が「汎用のサブエージェント用」になっていることを確認する
- **確認環境**: 開発環境
- **期待結果**: 「呼び出すサブエージェント（Step 11 工程①）」の見出しの下に委譲先固定なしの説明文が表示され、「プロンプトテンプレート」欄も「汎用のサブエージェント用」と表示されること
- **確認結果**:
- **試験実施日時**:

## 試験項目4: coding-test-2review経由の正当なmicro-impl-agent利用および他Step記述が変更されていないことの確認

- **状態、条件**: 同上SKILL.mdが最新の修正後の状態であること
- **確認項目**: ユーザーが、「呼び出す名前付きエージェント（すべて coding-test-2review 経由・Step 10）」欄と、Step11工程②〜④の記述、Step9（影響範囲再検討等の他Step）の記述が変更されていないことを確認する
- **確認手順**:
  1. `skills/fs-change-phase2-impl/SKILL.md` の「呼び出す名前付きエージェント（すべて coding-test-2review 経由・Step 10）」欄に `micro-impl-agent (aide-powers agent)` の行がそのまま存在することを確認する
  2. Step11本文の工程②（試験書作成）〜工程④（試験実行）の記述を通読し、本修正前と同じ内容であることを確認する
  3. 「呼び出す名前付きエージェント（Step 11 工程③）」欄に `manual-test-review-agent (aide-powers agent)` がそのまま存在することを確認する
- **確認環境**: 開発環境
- **期待結果**: coding-test-2review経由（Step10）欄には `micro-impl-agent (aide-powers agent)` がそのまま表示され、Step11工程②〜④・工程③のmanual-test-review-agent記述は変更されずそのまま表示されること
- **確認結果**:
- **試験実施日時**:
