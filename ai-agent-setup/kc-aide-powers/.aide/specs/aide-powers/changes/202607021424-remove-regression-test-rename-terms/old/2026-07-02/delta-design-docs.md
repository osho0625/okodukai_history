# 差分設計: docs-dev配下5件 ＋ docs/03-usage.md

対象ファイル: `docs-dev/02-ai-agent/04-agents/implementation-agents.md`, `docs-dev/02-ai-agent/03-common-skills/impl.md`, `docs-dev/02-ai-agent/02-phase-skills/bugfix.md`, `docs-dev/02-ai-agent/02-phase-skills/refactoring.md`, `docs-dev/02-ai-agent/01-workflows/07-refactoring.md`, `docs/03-usage.md`（計6件、すべて既存変更）

## 1. docs-dev/02-ai-agent/04-agents/implementation-agents.md

### 1-1. micro-impl-agent のモード表 — run_test の説明を更新

**before:**
```
| `run_test` | テスト実行。対象テスト + 全体リグレッションを実行し、結果を報告する。失敗時は実装またはテストを修正 | テスト結果報告 |
```

**after:**
```
| `run_test` | テスト実行。ユニットテストを実行し、結果を報告する。失敗時は実装またはテストを修正。既存テスト全実行（リグレッションテスト）は動作確認Stepで1回実施する設計に統一されている | テスト結果報告 |
```

**変更理由**: REQ-C-001・REQ-C-003。タスク単位の実装ループでの全体リグレッション実行は廃止され動作確認Stepに一本化されるため説明を修正する。用語も「対象テスト」→「ユニットテスト」に統一する。

---

## 2. docs-dev/02-ai-agent/03-common-skills/impl.md

### 2-1. impl-coding-standards の「メインプロセスの要点」— テスト実行ルールの説明を更新

**before:**
```
- **テスト関連ルール**: unittest 標準ライブラリ使用、pytest 禁止、モック・スタブ禁止
- **テスト実行ルール**: 対象テスト + 全体リグレッションの両方を実行
```

**after:**
```
- **テスト関連ルール**: unittest 標準ライブラリ使用、pytest 禁止、モック・スタブ禁止
- **テスト実行ルール**: ユニットテストを実行。既存テスト全実行（リグレッションテスト）は動作確認Stepで1回実施する設計に統一されている
```

**変更理由**: REQ-C-001・REQ-C-002・REQ-C-003。「対象テスト＋全体リグレッションの両方を実行」という説明を、動作確認Stepでの1回実施に統一された実態に合わせて修正する。用語も統一する。

### 2-2. multi-stage-code-review の「メインプロセスの要点」— パイプライン図の run_test 相当の記述を確認

**確認結果**: パイプライン図（```[タスク開始] → ... → micro-impl-agent: run_test モードでテスト実行 [タスク完了 ...]```）自体には「全体リグレッション」「対象テスト」等の廃止対象語は含まれていないため、パイプライン図の変更は不要。

---

## 3. docs-dev/02-ai-agent/02-phase-skills/bugfix.md

### 3-1. fs-bugfix-phase2-impl の責務 — 「リグレッションテスト」の記述を更新

**before:**
```
- 差分実装（ホワイトリスト3エージェントによる多段階コードレビュー）
- リグレッションテスト（バグ再現テスト + 既存テスト全実行）
- 設計書反映（`fix-design.md` を `doc-sync` 経由で既存設計書にマージ + `history.md` 初期作成）
```

**after:**
```
- 差分実装（ホワイトリスト3エージェントによる多段階コードレビュー）
- 動作確認Step（バグ再現テストを含む動作確認試験 + リグレッションテスト〔既存テスト全実行〕を1回実施）
- 設計書反映（`fix-design.md` を `doc-sync` 経由で既存設計書にマージ + `history.md` 初期作成）
```

**変更理由**: REQ-C-001・REQ-C-002。旧Step8（実装ループ）内で毎タスク実施していた既存テスト全実行と、旧Step9〜10（リグレッション確認＋動作検証）が動作確認Step（新Step9）に統合されたことを反映する。バグ再現テスト自体は実装タスクの一部として維持されるため、動作確認試験に含まれる旨を明記する。

### 3-2. Iron Law の代表ルール — 「リグレッションテスト必須」の記述を更新

**before:**
```
- **NO TASK PROCEEDS WITHOUT FULL REVIEW AND ALL TESTS PASSING**: レビュー全 PASS と全テストパスなしに次タスクへ進めない。
- **NO TASK MERGING — EVER**: 差分タスクの統合実装を禁止。1 タスク = 1 回の `multi-stage-code-review` 呼び出し。
- **リグレッションテスト必須**: バグ再現テストの作成と既存テスト全実行を欠かさない。
- REJECTED 後の再 QA 省略禁止。
```

**after:**
```
- **NO TASK PROCEEDS WITHOUT FULL REVIEW AND ALL TESTS PASSING**: レビュー全 PASS とユニットテストパスなしに次タスクへ進めない。
- **NO TASK MERGING — EVER**: 差分タスクの統合実装を禁止。1 タスク = 1 回の `multi-stage-code-review` 呼び出し。
- **バグ再現テスト必須・リグレッションテストは動作確認Stepで1回**: バグ再現テストの作成は各実装タスクで欠かさない。既存テスト全実行（リグレッションテスト）は動作確認Step（Step9）で1回実施する。
- REJECTED 後の再 QA 省略禁止。
```

**変更理由**: REQ-C-001・REQ-C-002・REQ-C-003。「リグレッションテスト必須」が実装ステップ内での毎タスク実行を示唆する表現だったため、バグ再現テスト（タスク単位・必須）と既存テスト全実行（動作確認Stepで1回）を明確に分離した表現に修正する。「全テストパス」も「ユニットテストパス」に統一する。

### 3-3. 主要な共通スキル呼び出し — 変更なしの確認

**確認結果**: 「主要な共通スキル呼び出し」一覧（`multi-stage-code-review`, `impl-task-planning`, `impl-coding-standards` 等の列挙）自体には廃止対象語が含まれていないため、変更不要。

---

## 4. docs-dev/02-ai-agent/02-phase-skills/refactoring.md

### 4-1. fs-refactoring-phase5-impl の責務 — 「各タスク完了時にセーフティネット」の記述を更新

**before:**
```
## fs-refactoring-phase5-impl

### 責務

差分タスクを 1 つずつ実装する。**各タスク完了時にセーフティネット（既存テスト全実行）を必ず実行**し、
フェーズ1で記録した基準値と照合する。FAIL 数増加・スキップ数の不自然な変化があれば即座に修正する。
ホワイトリスト3エージェントによる多段階コードレビューも実施する。パイプライン全体は
[`multi-stage-code-review` 共通スキル](../03-common-skills/impl.md#multi-stage-code-review)
が司る。詳細はそちらを参照。

### Iron Law の代表ルール

- **NO TASK PROCEEDS WITHOUT SAFETY NET PASS**: 各タスク完了ごとのセーフティネット PASS なしに次タスクへ進めない。
```

**after:**
```
## fs-refactoring-phase5-impl

### 責務

差分タスクを 1 つずつ実装する。ホワイトリスト3エージェントによる多段階コードレビューを実施する。
パイプライン全体は
[`multi-stage-code-review` 共通スキル](../03-common-skills/impl.md#multi-stage-code-review)
が司る。詳細はそちらを参照。全タスク完了後の動作確認Stepで、セーフティネット（既存テスト全実行）を
**1 回実行**し、フェーズ1で記録した基準値と照合する。FAIL 数増加・スキップ数の不自然な変化があれば
実装ループへ差し戻して修正する。

### Iron Law の代表ルール

- **NO WORKFLOW COMPLETION WITHOUT SAFETY NET PASS**: 動作確認Stepでのセーフティネット PASS なしにワークフローを完了できない。
```

**変更理由**: REQ-C-001・REQ-C-002。「各タスク完了時にセーフティネットを必ず実行」という実装ステップ内の毎回実行を廃止し、動作確認Stepでの1回実施に統一する。Iron Law の表現も「各タスク完了ごと」から「動作確認Stepでの1回確認によりワークフロー完了をブロックする」解釈に修正する。

### 4-2. 主要な共通スキル呼び出し — 変更なしの確認

**確認結果**: 「主要な共通スキル呼び出し」一覧自体には廃止対象語が含まれていないため、変更不要。

---

## 5. docs-dev/02-ai-agent/01-workflows/07-refactoring.md

### 5-1. 概要文 — 「セーフティネット（既存テスト全実行）で外部振る舞いの保持を機械的に保証する」の維持確認

**確認結果**: ワークフロー概要文自体（「外部振る舞いを変えずに内部構造を改善するためのワークフロー。セーフティネット（既存テスト全実行）で外部振る舞いの保持を機械的に保証する。」）は、セーフティネットという仕組み自体（動作確認Stepでの1回実施に変更後も存続する）の説明であり、実施タイミングに言及していないため変更不要。

### 5-2. ゲート — 「セーフティネット（フェーズ5のタスクごと）」の見出しと表を更新

**before:**
```
### セーフティネット（フェーズ5のタスクごと）

各タスク完了時に既存テストを全実行し、フェーズ1で記録した基準値と照合する。

| 結果 | 判定 |
|---|---|
| 基準値と完全一致 | PASS（次のタスクへ） |
| FAIL 数増加 | 外部振る舞いが変わった証拠。即座に修正 |
| スキップ数の不自然な変化 | テストが意図せず無効化された疑い。調査して修正 |

セーフティネット PASS なしに次のタスクへ進めない。Iron Law。
```

**after:**
```
### セーフティネット（フェーズ5の動作確認Stepで1回）

全タスク完了後の動作確認Stepで既存テストを全実行し、フェーズ1で記録した基準値と照合する。

| 結果 | 判定 |
|---|---|
| 基準値と完全一致 | PASS（動作確認試験へ進み、ワークフロー完了へ） |
| FAIL 数増加 | 外部振る舞いが変わった証拠。実装ループへ差し戻して修正 |
| スキップ数の不自然な変化 | テストが意図せず無効化された疑い。調査して修正 |

セーフティネット PASS なしにワークフローを完了できない。Iron Law。
```

**変更理由**: REQ-C-001・REQ-C-002。各タスク完了時の毎回実行から、全タスク完了後の動作確認Stepでの1回実施に変更されたことを反映する。

### 5-3. Iron Law — 「NO TASK PROCEEDS WITHOUT SAFETY NET PASS」の記述を更新

**before:**
```
- **NO TASK PROCEEDS WITHOUT SAFETY NET PASS**: 各タスク完了ごとのセーフティネット全実行 PASS なしに次タスクへ進めない
```

**after:**
```
- **NO WORKFLOW COMPLETION WITHOUT SAFETY NET PASS**: 動作確認Stepでのセーフティネット全実行 PASS なしにワークフローを完了できない
```

**変更理由**: 上記5-2と整合させる。

---

## 6. docs/03-usage.md

### 6-1. §5.6 リファクタリングワークフロー — セーフティネットの説明を更新

**before:**
```
入口で設計書ゲートを通したあと、既存テストを全実行して PASS / FAIL / スキップ数を
**セーフティネット基準** として記録します。リファクタリング実装中は各タスク完了
ごとに既存テストを再実行し、基準値と比較することで「外部振る舞いが変わっていない」
ことを機械的に保証します。
```

**after:**
```
入口で設計書ゲートを通したあと、既存テストを全実行して PASS / FAIL / スキップ数を
**セーフティネット基準** として記録します。リファクタリング実装がすべて完了した後の
動作確認Stepで既存テストを再実行し、基準値と比較することで「外部振る舞いが変わっていない」
ことを機械的に保証します。
```

**変更理由**: REQ-C-001・REQ-C-002。「各タスク完了ごとに既存テストを再実行」という実施タイミングの記述を、実際の仕様（動作確認Stepで1回実施）に合わせて修正する。

### 6-2. §5.7 バグ修正ワークフロー — リグレッションテストの説明を更新

**before:**
```
ヒアリング後にコードとテストを調査して根本原因を特定し、原因 / 修正方針 /
修正種別の三つ組を利用者と合意します。差分設計書には **リグレッションテスト**
（バグ再現テスト）の設計を必ず含め、各タスク完了時には **既存テスト全実行** で
他の機能を壊していないことを確認します。
```

**after:**
```
ヒアリング後にコードとテストを調査して根本原因を特定し、原因 / 修正方針 /
修正種別の三つ組を利用者と合意します。差分設計書には **リグレッションテスト**
（バグ再現テスト）の設計を必ず含め、実装完了後の動作確認Stepで **既存テスト全実行** を
1 回実施し、他の機能を壊していないことを確認します。
```

**変更理由**: REQ-C-001・REQ-C-002。「各タスク完了時に既存テスト全実行」という記述を、動作確認Step時点での1回実施という実態に合わせて修正する。「リグレッションテスト（バグ再現テスト）の設計」自体（テスト内容の設計）は実行タイミングの話ではないため文言を維持するが、直後の全実行タイミングの記述と混同しないよう「実装完了後の動作確認Step」を明示する。

### 6-3. §8.1 ワークフロー別の傾向 — バグ修正WF・リファクタリングWFの主因説明を更新

**before:**
```
| バグ修正ワークフロー | 原因分析、リグレッションテスト設計、既存テスト全実行、ドキュメント同期 |
| リファクタリングワークフロー | セーフティネット記録、各タスクごとの既存テスト全実行 |
```

**after:**
```
| バグ修正ワークフロー | 原因分析、リグレッションテスト設計、動作確認Stepでの既存テスト全実行、ドキュメント同期 |
| リファクタリングワークフロー | セーフティネット記録、動作確認Stepでの既存テスト全実行 |
```

**変更理由**: REQ-C-001・REQ-C-002。トークン消費傾向表の記述も、実施タイミングが動作確認Stepでの1回実施に統一されたことを反映する。
