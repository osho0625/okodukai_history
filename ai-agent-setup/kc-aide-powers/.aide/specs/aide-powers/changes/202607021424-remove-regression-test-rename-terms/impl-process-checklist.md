# 工程チェック表

本チェック表は delta-task-list.md の全8タスク（既存変更8件。regression-test-prompt.md 4件 + SKILL.md 4件）について、工程（implement, write_test, run_test, design_review, code_review）ごとに行を作成する。

**行キー生成ルール:** 本修正の8タスクはいずれも既存Markdownファイル（regression-test-prompt.md / SKILL.md）の構造・記述修正であり、サブタスク分割（publicメソッド単位）の概念が存在しない。したがって各タスクは親タスクIDのみで行キーを生成する（例: B-001::implement）。

**非プログラム成果物の工程判定:** 本修正の8ファイルは全てMarkdownのスキル定義ファイル（プロンプトテンプレート・SKILL.md本体）であり、実行可能なプログラムコードではない。dev-environment.md §7.4 の確定方針によりリポジトリに自動テストフレームワークは導入されていない。したがって各タスクは「実装（implement）」「設計準拠レビュー（design_review）」のみを実工程行とし、「テスト実装（write_test）」「テスト実行（run_test）」「コード品質レビュー（code_review）」は ➖skip とする（理由: 非プログラム成果物のMarkdown記述変更であり、専用テストコード・自動テスト実行・コード品質観点のレビュー対象が存在しないため）。品質担保は fix-design.md「既存テストへの影響」記載の方法（diff確認・構造パターン統一確認）に基づき design_review 工程で行う。

## チェック表

| タスクID::工程 | 状態 | 備考 |
|---|---|---|
| B-001::implement | ✅ done | skills/fs-bugfix-phase2-impl/regression-test-prompt.md の「委譲先エージェント」「プレースホルダー」セクションを削除し、コードブロックを解放してトップレベル構造に統一した（プロンプト本文は無変更）。実装完了、design_review待ち |
| B-001::write_test | ➖ skip | 非プログラム成果物（Markdownプロンプトテンプレート）のため専用テストコード作成は対象外 |
| B-001::run_test | ➖ skip | 自動テストフレームワーク未導入（dev-environment.md §7.4）。品質担保はdiff確認（design_review）で実施 |
| B-001::design_review | ✅ done | PASS（検査対象1ファイル, 差分0件）。design-review-agentによる再検証済み。fix-design.md 変更対象1のbefore→after全項目を実ファイルと1項目ずつ照合: ①「## 委譲先エージェント」セクション削除済み ②「## プレースホルダー（FSが実データで置き替える）」セクション削除済み ③「## 実行内容」見出しと「micro-impl-agent (aide-powers agent) を以下のプロンプトで起動する:」の一文削除済み ④コードブロック(```)が外され「### タスク情報」〜「## 報告フォーマット」がトップレベル構造化済み ⑤「## 出力」セクションは維持 ⑥冒頭見出し・説明文は無変更。プロンプト本文（### タスク情報〜## 報告フォーマット）はgit diff（導入時コミット3a5ea31→現行）で確認し、セクション削除・コードブロック解放以外の追加・変更が一切ないことを確認（一字一句無変更）。内部ロジック意図検証: 本タスクは構造変更のみが目的であり、テスト実行指示等のロジック内容自体は変更対象外。ダミー実装・先送りマーカーなし |
| B-001::code_review | ➖ skip | 非プログラム成果物（Markdownプロンプトテンプレート）のため対象外。品質観点は design_review で構造統一確認も含めて検証する |
| B-002::implement | ✅ done | skills/fs-change-phase2-impl/regression-test-prompt.md の「委譲先エージェント」「プレースホルダー」セクションを削除し、コードブロックを解放してトップレベル構造に統一した（`{{changes_dir}}` 等のプロンプト本文は無変更）。実装完了、design_review待ち |
| B-002::write_test | ➖ skip | 同上 |
| B-002::run_test | ➖ skip | 同上 |
| B-002::design_review | ✅ done | PASS（検査項目数: 7, 差分0件）。design-review-agentによる再検証（過去記載を鵜呑みにせず実施）。fix-design.md 変更対象2 の before→after 全項目を実ファイルと1つずつ照合: ①冒頭見出し「# リグレッションテスト実行エージェント（変更WF用）」と説明文は無変更 ②「## 委譲先エージェント」セクション削除済み ③「## プレースホルダー（FSが実データで置き替える）」セクション削除済み ④「## 実行内容」見出しと「micro-impl-agent (aide-powers agent) を以下のプロンプトで起動する:」の一文削除済み ⑤コードブロック(```)が外され「### タスク情報」〜「## 報告フォーマット」がトップレベル構造に引き上げ済み（見出しレベル・内容とも変更なし） ⑥「## 出力」セクションはそのまま残存 ⑦プロンプト本文（`{{dev_environment_path}}`等の変数含む）はgit diff（3a5ea31→現行）で構造改変部分（委譲先/プレースホルダー/実行内容見出し/コードブロック）以外に差分なしと確認、本文一字一句無変更。B-001（fs-bugfix-phase2-impl）・B-003（fs-impl-phase4-execution）と見出し文言・Step番号・ワークフロー名を除き同一構造パターンであることも確認 |
| B-002::code_review | ➖ skip | 非プログラム成果物のため対象外。品質観点は design_review で検証する |
| B-003::implement | ✅ done | skills/fs-impl-phase4-execution/regression-test-prompt.md の「委譲先エージェント」「プレースホルダー」セクション削除・コードブロック解放を実施した。トップレベルに「### タスク情報」〜「## 報告フォーマット」を引き上げ、「## 出力」は維持。プロンプト本文（`{{spec_dir}}` 等）は無変更 |
| B-003::write_test | ➖ skip | 同上 |
| B-003::run_test | ➖ skip | 同上 |
| B-003::design_review | ✅ done | PASS（検査項目数: 7, 差分: 0）。design-review-agentによる再検証済み。fix-design.md 変更対象3のbefore→after全項目を実ファイルと1項目ずつ照合: ①冒頭見出し「# リグレッションテスト実行エージェント（実装WF用）」と説明文4行は無変更 ②「## 委譲先エージェント」セクション削除済み（grep確認: 該当文言なし） ③「## プレースホルダー（FSが実データで置き替える）」セクション削除済み ④「## 実行内容」見出しと「micro-impl-agent (aide-powers agent) を以下のプロンプトで起動する:」の一文削除済み ⑤コードブロック(```)が外され「### タスク情報」〜「## 報告フォーマット」がトップレベル構造化済み（見出しレベル・内容は無変更） ⑥「## 出力」セクションは維持 ⑦ファイル全体が「説明文→###タスク情報→###実行モード→###テスト実行コマンド（必須）→###開発環境情報→##テスト実行ルール→##報告フォーマット→##出力」の同型構造になっていることを確認。プロンプト本文（`{{spec_dir}}`, `{{dev_environment_path}}`, 「呼び出し元ワークフロー: 実装WF（fs-impl-phase4-execution Step2）」等含む）はgit diff（導入コミット3a5ea31→現行）で一字一句無変更を確認。B-001適用後（fs-bugfix-phase2-impl）と見出し文言・変数名・Step番号を除き完全に同一の構造パターンであることを確認。内部ロジック意図検証: 本タスクは構造変更のみが目的であり、テスト実行指示等のロジック内容自体は変更対象外で維持されている。ダミー実装・先送りマーカーなし。importルール: N/A（Markdownスキル定義ファイルのため対象外） |
| B-003::code_review | ➖ skip | 非プログラム成果物のため対象外。品質観点は design_review で検証する |
| B-004::implement | ✅ done | skills/fs-refactoring-phase5-impl/regression-test-prompt.md の「委譲先エージェント」「プレースホルダー（`{{safety_net_baseline}}` 含む4変数）」セクションを削除し、「## 実行内容」見出しと起動一文を削除、コードブロックを解放して「### タスク情報」〜「## 報告フォーマット」（開始前基準セクション・判定ロジックを含む）をトップレベルに引き上げた。「## 出力」（開始前基準との比較結果を含む）は維持。基準比較機能・プロンプト本文は無変更。実装完了、design_review待ち |
| B-004::write_test | ➖ skip | 同上 |
| B-004::run_test | ➖ skip | 同上 |
| B-004::design_review | ✅ done | PASS（検査対象1ファイル, 差分0件）。design-review-agentが本タスクを再検証。fix-design.md 変更対象4のbefore→after全項目を実ファイルと1項目ずつ照合: ①「## 委譲先エージェント」セクション削除済み ②「## プレースホルダー（FSが実データで置き替える）」セクション（`{{safety_net_baseline}}`含む4変数）削除済み ③「## 実行内容」見出しと「micro-impl-agent (aide-powers agent) を以下のプロンプトで起動する:」の一文削除済み ④コードブロック(```)が外され「### タスク情報」〜「## 報告フォーマット」（開始前基準セクション・判定ロジックを含む）がトップレベル構造化済み（見出しレベル・内容も変更前と一致） ⑤「## 出力」セクション（開始前基準との比較結果の項目含む）は維持 ⑥冒頭見出し・説明文は無変更。他3ファイル（B-001〜B-003）との構造差異（`### 開始前基準（比較対象）`セクション・`{{safety_net_baseline}}`変数・テスト実行ルール内の基準比較判定ロジック・報告フォーマット/出力内の基準比較項目）はfix-design.mdの指示通り維持されており、統一されていないことを確認（統一していたら誤り）。git diff（作業ツリー差分）で変更範囲を確認し、削除された行は「## 委譲先エージェント」節、「## プレースホルダー」節、「## 実行内容」見出し+起動文、開閉のコードブロックフェンス2箇所のみであり、それ以外（「### タスク情報」〜「## 報告フォーマット」のプロンプト本文、基準比較ロジック、「## 出力」節）は一字一句無変更であることを確認。内部ロジック意図検証: 本タスクは構造変更のみが目的であり、リグレッションテスト実行手順・基準比較ロジックの内容自体は変更対象外（fix-design.mdの意図通り）。ダミー実装・先送りマーカーなし。importルール: N/A（Markdownファイル） |
| B-004::code_review | ➖ skip | 非プログラム成果物のため対象外。品質観点は design_review で検証する |
| B-005::implement | ✅ done | skills/fs-bugfix-phase2-impl/SKILL.md のIntegration節見出し変更（「呼び出す名前付きエージェント（Step 9 工程①）」→「呼び出すサブエージェント（Step 9 工程①）」）、委譲先固定記述を汎用表現（委譲先固定なし）へ置き換え、プロンプトテンプレート欄の「micro-impl-agent 用」→「汎用のサブエージェント用」置き換えを実施した。coding-test-2review経由欄・Step9工程③（manual-test-review-agent）欄・Step9本文・工程②〜④記述は無変更。実装完了、design_review待ち |
| B-005::write_test | ➖ skip | 非プログラム成果物（Markdownスキル定義ファイル）のため専用テストコード作成は対象外 |
| B-005::run_test | ➖ skip | 自動テストフレームワーク未導入。品質担保はdiff確認（design_review）で実施 |
| B-005::design_review | ✅ done | PASS（検査対象1ファイル, 差分0件）。design-review-agent により再レビュー実施済み（既存記載は再検証のうえ更新）。fix-design.md 変更対象5のbefore→after全項目を実ファイル照合で確認: ①Integration節見出し「呼び出す名前付きエージェント（Step 9 工程①）」→「呼び出すサブエージェント（Step 9 工程①）」に変更済み（現ファイル465行目）、②`micro-impl-agent (aide-powers agent)` 固定記述行が「委譲先は具体的なエージェント名で固定しない。regression-test-prompt.md の内容（プレースホルダー置換済み）をそのままプロンプトとしてサブエージェントに渡し、プラットフォームのツールマップに従って汎用のサブエージェントを起動する（Step 9 工程①: リグレッションテスト実行。工程②〜④より先行）」に一字一句一致で置換済み（466行目）、③プロンプトテンプレート欄の `regression-test-prompt.md` 説明中「micro-impl-agent 用」が「汎用のサブエージェント用」に置換済み（461行目、fix-design.md after と完全一致）。git diff（HEAD比較）で本ファイルの変更範囲がこの3行replace（3行削除+3行追加、計6行）のみであることを確認し、対象外部分（「呼び出す名前付きエージェント（すべてcoding-test-2review経由・Step 8）」欄=micro-impl-agent/design-review-agent/code-review-agent固定記述、「呼び出す名前付きエージェント（Step 9 工程③）」manual-test-review-agent欄、Step9本文の工程①〜④記述、coding-test-2review経由欄）は一切変更されていないことを確認。同一ファイル内で「委譲先固定なし」表現と「micro-impl-agent用」表現が併存する内部矛盾も解消済み（grep で `micro-impl-agent 用` の残存なしを確認）。importルール: N/A（Markdownファイルのため対象外） |
| B-005::code_review | ➖ skip | 非プログラム成果物のため対象外。品質観点は design_review で検証する |
| B-006::implement | ✅ done | skills/fs-change-phase2-impl/SKILL.md のIntegration節見出しを「呼び出す名前付きエージェント（Step 11 工程①）」→「呼び出すサブエージェント（Step 11 工程①）」に変更し、委譲先固定記述を汎用表現（委譲先を固定しない旨の説明文）に置き換えた。同節内プロンプトテンプレート欄の `regression-test-prompt.md` の説明を「micro-impl-agent 用」→「汎用のサブエージェント用」に置き換えた。他欄（coding-test-2review経由欄・工程③のmanual-test-review-agent欄）およびStep11本文・工程②〜④記述は無変更。実装完了、design_review待ち |
| B-006::write_test | ➖ skip | 同上 |
| B-006::run_test | ➖ skip | 同上 |
| B-006::design_review | ✅ done | PASS（検査項目数: 6, 差分0件）。design-review-agentによる再検証済み。fix-design.md 変更対象6のbefore→after全項目を実ファイルと1項目ずつ照合: ①Integration節見出し「呼び出す名前付きエージェント（Step 11 工程①）」→「呼び出すサブエージェント（Step 11 工程①）」に変更済み ②`micro-impl-agent (aide-powers agent)` 固定記述行が「委譲先は具体的なエージェント名で固定しない。regression-test-prompt.md の内容（プレースホルダー置換済み）をそのままプロンプトとしてサブエージェントに渡し、プラットフォームのツールマップに従って汎用のサブエージェントを起動する（Step 11 工程①: リグレッションテスト実行。工程②〜④より先行）」に一字一句一致で置換済み ③プロンプトテンプレート欄の `regression-test-prompt.md` 説明中「micro-impl-agent 用」が「汎用のサブエージェント用」に置換済み（fix-design.md after と完全一致）。`git diff HEAD -- skills/fs-change-phase2-impl/SKILL.md` で本ファイルの変更が上記②③の2箇所（3行削除+3行追加）のみであることを確認し、「呼び出す名前付きエージェント（すべてcoding-test-2review経由・Step10）」欄（micro-impl-agent含む。Step10専用のため変更対象外）・「呼び出す名前付きエージェント（Step 11 工程③）」（manual-test-review-agent）欄・Step11本文（工程①〜④の記述）は一字一句無変更であることを確認。同一ファイル内に「呼び出す名前付きエージェント」見出しがStep10用に残っているが、これはStep10（coding-test-2review経由）用であり変更対象外のため内部矛盾なし。B-002（regression-test-prompt.md、委譲先エージェント節削除済み）との整合も確認。内部ロジック意図検証: 委譲先固定なしの説明文が実際に「規定のエージェント名を書かない」設計意図を満たしていることを文面で確認。importルール: N/A（Markdownファイル） |
| B-006::code_review | ➖ skip | 非プログラム成果物のため対象外。品質観点は design_review で検証する |
| B-007::implement | ✅ done | skills/fs-impl-phase4-execution/SKILL.md のIntegration節見出しを「呼び出す名前付きエージェント（Step 2 工程①）」→「呼び出すサブエージェント（Step 2 工程①）」に変更し、委譲先固定記述を汎用表現（委譲先を固定しない旨の説明文）に置き換えた。同節内プロンプトテンプレート欄の `regression-test-prompt.md` の説明を「micro-impl-agent 用」→「汎用のサブエージェント用」に置き換えた。他欄（coding-test-2review経由欄・工程③のmanual-test-review-agent欄）およびStep2本文・工程②〜④記述は無変更。実装完了、design_review待ち |
| B-007::write_test | ➖ skip | 同上 |
| B-007::run_test | ➖ skip | 同上 |
| B-007::design_review | ✅ done | PASS（検査対象1ファイル, 差分0件）。design-review-agentが実ファイルを再検証（既存記載は鵜呑みにせず再実施）。fix-design.md 変更対象7のbefore→after全項目を実ファイルと1項目ずつ照合: ①Integration節見出し「呼び出す名前付きエージェント（Step 2 工程①）」→「呼び出すサブエージェント（Step 2 工程①）」に変更済み ②`micro-impl-agent (aide-powers agent)` 固定記述行が「委譲先は具体的なエージェント名で固定しない。regression-test-prompt.md の内容（プレースホルダー置換済み）をそのままプロンプトとしてサブエージェントに渡し、プラットフォームのツールマップに従って汎用のサブエージェントを起動する（Step 2 工程①: リグレッションテスト実行。工程②〜④より先行）」に一字一句一致で置換済み ③プロンプトテンプレート欄の `regression-test-prompt.md` 説明中「micro-impl-agent 用」が「汎用のサブエージェント用」に置換済み（fix-design.md after と完全一致）。`git diff HEAD -- skills/fs-impl-phase4-execution/SKILL.md`（コミット3a5ea31→現行）で本ファイルの変更が上記②③の2箇所のみであることを確認し、「呼び出す名前付きエージェント（Step 2 工程③）」（manual-test-review-agent）欄・「呼び出す名前付きエージェント（すべて coding-test-2review 経由・Step 1）」欄・Step2本文（工程①〜④の記述）は一字一句無変更であることを確認。「coding-test-2review経由・Step 1」欄に micro-impl-agent 記述が残るが、Step1（実装タスクループ）専用でスコープが異なり内部矛盾ではないことを確認（fix-design.mdも変更対象外と明記）。内部ロジック意図検証: 委譲先固定なしの説明文が「規定のエージェント名を書かない」設計意図を満たしていることを文面で確認。ダミー実装・先送りマーカーなし。importルール: N/A（Markdownファイルのため対象外） |
| B-007::code_review | ➖ skip | 非プログラム成果物のため対象外。品質観点は design_review で検証する |
| B-008::implement | ✅ done | skills/fs-refactoring-phase5-impl/SKILL.md のIntegration節見出しを「呼び出す名前付きエージェント（Step 2）」→「呼び出すサブエージェント（Step 2）」に変更し、委譲先固定記述を汎用表現（委譲先を固定しない旨の説明文）に置き換えた。同節内サブエージェントプロンプト欄の `regression-test-prompt.md` の説明を「micro-impl-agent 用」→「汎用のサブエージェント用」に置き換えた。Step2本文中の `` `micro-impl-agent` を起動し `` を「サブエージェントを起動し」に置き換えた（開始前基準比較等の他文言は無変更）。Step3工程②（manual-test-review-agent欄）・coding-test-2review経由欄は無変更。実装完了、design_review待ち |
| B-008::write_test | ➖ skip | 同上 |
| B-008::run_test | ➖ skip | 同上 |
| B-008::design_review | ✅ done | PASS（検査対象1ファイル, 差分0件）。design-review-agentが本タスクを再検証（既存記載は信用せず、fix-design.md本文とgit diff 3a5ea31→現行を自ら再照合）。変更対象8のbefore→after全3項目を実ファイルと1項目ずつ照合: ①Integration節見出し「呼び出す名前付きエージェント（Step 2）」→「呼び出すサブエージェント（Step 2）」に変更済み、`micro-impl-agent (aide-powers agent)` 固定記述行が「委譲先は具体的なエージェント名で固定しない。regression-test-prompt.md の内容（プレースホルダー置換済み）をそのままプロンプトとしてサブエージェントに渡し、プラットフォームのツールマップに従って汎用のサブエージェントを起動する（Step 2: リグレッションテスト実行。開始前基準〔セーフティネットベースライン〕との比較報告を含む）」に一字一句一致で置換済み ②サブエージェントプロンプト欄の `regression-test-prompt.md` 説明中「micro-impl-agent 用」が「汎用のサブエージェント用」に置換済み（他文言「phase1-statusのセーフティネット基準との比較報告を含む」は無変更） ③Step2本文中「プロンプトとして `micro-impl-agent` を起動し」が「プロンプトとしてサブエージェントを起動し」に置換済み（開始前基準比較等の他文言は一字一句無変更）。`git diff 3a5ea31 -- skills/fs-refactoring-phase5-impl/SKILL.md` で変更範囲が上記3ヶ所（2ハンク）のみであることを確認し、Step3工程②（manual-test-review-agent）欄、および「呼び出す名前付きエージェント（すべて coding-test-2review 経由・Step 1）」欄（micro-impl-agent/design-review-agent/code-review-agent固定記述）は一切変更されていないことを確認。同一ファイル内で「委譲先固定なし」表現と「micro-impl-agent用」表現が併存する内部矛盾も解消済み（grepで残存なしを確認、行107・238のmicro-impl-agentはcoding-test-2review経由のStep1用固定記述であり変更対象外）。内部ロジック意図検証: 本タスクは委譲先固定記述の削除・言い換えのみが目的であり、リグレッションテスト実行手順・基準比較ロジックの内容自体は変更対象外（fix-design.mdの意図通り維持）。ダミー実装・先送りマーカーなし。importルール: N/A（Markdownファイルのため対象外） |
| B-008::code_review | ➖ skip | 非プログラム成果物のため対象外。品質観点は design_review で検証する |

## 集計

- 総行数: 40行（8タスク × 5工程）
- ➖ skip 行数: write_test 8行 + run_test 8行 + code_review 8行 = 24行（全て非プログラム成果物・自動テスト未導入のため）
- ⬜ 未着手 行数: 10行（implement 3行〔B-006〜B-008〕+ design_review 7行）
- 🔄 in-progress 行数: 1行（B-005::implement）
- ✅ done 行数: 5行（B-001〜B-004::implement, B-002::design_review）
