# 差分タスクリスト

## 変更ID
202606252140-phase-skill-duty-declaration

## タスク一覧サマリー

| カテゴリ | 件数 |
|---|---|
| 新規追加 | 1 |
| 既存変更 | 1 |
| GUI実装 | 0 |
| リグレッションテスト（手動確認） | 3 |
| **合計** | **5** |

---

## 依存関係グラフ

```
D-001 (phase-skill-rules.md セクション追加)  ── 依存先なし
D-002 (version.json 値変更)                   ── 依存先なし
D-R-001 (配布メカニズム動作確認)              ── D-001, D-002 完了後
D-R-002 (責務宣言実行確認)                    ── D-R-001 完了後（配布後に確認）
D-R-003 (既存動作非影響確認)                  ── D-R-001 完了後（配布後に確認）
```

**並列実行可能**: D-001 と D-002 は相互依存なし。同時実行可能。

---

## タスク詳細

### D-001: phase-skill-rules.md — 責務宣言ルールセクション追加

| 項目 | 内容 |
|---|---|
| タスクID | D-001 |
| 変更種別 | 新規追加（セクション追加） |
| 対象ファイル | `skills/using-aide-powers/references/phase-skill-rules.md` |
| テストファイル | N/A（手動確認） |
| 依存先タスク | なし |
| 設計参照 | delta-design.md §1 |

#### 実装内容

- 「前処理の phase-report-check (verify) 必須実行」セクションの直後（`---` 区切り線の後）、「コンテキスト制約時の対応」セクションの直前に新規セクション「各Step・前処理・後処理の実行前の責務宣言」を挿入する
- delta-design.md §1 の after ブロックに記載された内容を正確に追加する
- 既存セクション（before 部分）は一切変更しない

#### 確認観点

- [ ] 追加位置が正しいこと（「前処理の phase-report-check (verify) 必須実行」の直後、「コンテキスト制約時の対応」の直前）
- [ ] 既存セクション（before部分）が無変更であること
- [ ] 責務宣言文言が引用ブロック内に正確に記載されていること
- [ ] 3箇所のタイミング（前処理・各Step・後処理）が番号付きリストで明記されていること
- [ ] 「省略禁止」サブセクションが存在し、累積省略禁止が明記されていること
- [ ] `---` 区切り線で前後セクションと適切に分離されていること

---

### D-002: version.json — バージョンインクリメント

| 項目 | 内容 |
|---|---|
| タスクID | D-002 |
| 変更種別 | 既存変更（値変更） |
| 対象ファイル | `skills/using-aide-powers/references/version.json` |
| テストファイル | N/A（手動確認） |
| 依存先タスク | なし |
| 設計参照 | delta-design.md §2 |

#### 実装内容

- `version` フィールドを `14` → `15` に変更する
- `updated` フィールドを実装日の日付に更新する

#### 確認観点

- [ ] version が 15 であること
- [ ] updated が実装日の日付であること
- [ ] JSON 構文が正しいこと（パースエラーなし）

---

### D-R-001: 手動確認 — 配布メカニズム動作

| 項目 | 内容 |
|---|---|
| タスクID | D-R-001 |
| 確認種別 | 手動テスト |
| 依存先タスク | D-001, D-002 |
| 設計参照 | impact-analysis.md §4 手動テスト対象 #1 |

#### 確認手順

1. D-001, D-002 の実装完了後、`setup.bat` を実行してグローバル領域にデプロイする
2. 新規セッションで `using-aide-powers` を起動する
3. `.aide/references/version.json` が version=15 に更新されることを確認する
4. `.aide/references/phase-skill-rules.md` に新規セクションが含まれることを確認する

#### 確認観点

- [ ] version 比較 → コピー → 配布が正常に動作すること
- [ ] 配布先ファイルに新規追加セクションが含まれること

---

### D-R-002: 手動確認 — 責務宣言実行確認

| 項目 | 内容 |
|---|---|
| タスクID | D-R-002 |
| 確認種別 | 手動テスト |
| 依存先タスク | D-R-001（配布完了後） |
| 設計参照 | impact-analysis.md §4 手動テスト対象 #2 |

#### 確認手順

1. 配布完了後、任意のフェーズスキル（例: `fs-change-phase1-analysis`）を実行する
2. 各Step開始前に責務宣言が出力されることを確認する

#### 確認観点

- [ ] AIエージェントが各Step実行前に責務宣言を出力すること
- [ ] 宣言文言が定義どおりであること

---

### D-R-003: 手動確認 — 既存動作非影響確認

| 項目 | 内容 |
|---|---|
| タスクID | D-R-003 |
| 確認種別 | 手動テスト（リグレッション確認） |
| 依存先タスク | D-R-001（配布完了後） |
| 設計参照 | impact-analysis.md §4 手動テスト対象 #3 |

#### 確認手順

1. 配布完了後、任意のフェーズスキルを実行する
2. 前処理（phase-report-check verify）が従来通り動作することを確認する
3. 責務宣言追加以外の動作に変化がないことを確認する

#### 確認観点

- [ ] phase-report-check (verify) が正常に動作すること（直前フェーズ確認・違反履歴確認・ルール厳守宣言）
- [ ] 既存セクションのルールが引き続き適用されていること

---

## 実装順序（推奨）

| 順序 | タスクID | 備考 |
|---|---|---|
| 1 | D-001, D-002 | 並列実行可能（相互依存なし） |
| 2 | D-R-001 | D-001, D-002 完了後に setup.bat 実行 |
| 3 | D-R-002, D-R-003 | D-R-001 完了後に並列確認可能 |
