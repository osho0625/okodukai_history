# 差分設計書

## 変更概要
- **変更ID**: 202606252140-phase-skill-duty-declaration
- **目的**: 全フェーズスキル（`fs-*`）の各Step・前処理・後処理の実行前に責務宣言を必須化し、手順省略・独自解釈を抑止する
- **対応方針**: 追加で対応（OCP準拠、既存セクション無変更）

---

## 1. 新規追加: phase-skill-rules.md — 責務宣言ルールセクション

### 対象ファイル
`skills/using-aide-powers/references/phase-skill-rules.md`

### 追加位置
「前処理の phase-report-check (verify) 必須実行」セクションの直後（`---` 区切り線の後）、「コンテキスト制約時の対応」セクションの直前

### before（追加位置の前後コンテキスト）

```markdown
## 前処理の phase-report-check (verify) 必須実行

全フェーズスキルの前処理で `phase-report-check (aide-powers skill: verify)` を activate して実行することは**省略禁止**である。verify モードには以下が含まれており、これらを毎フェーズ開始時に必ず実行しなければならない:

- 直前フェーズの完了状態確認
- 違反履歴ファイルの確認とユーザーへの連絡（違反履歴がある場合）
- ルール厳守宣言（本フェーズをルールに従って実行する宣言）

「先頭フェーズだから前フェーズ確認は不要」であっても、違反履歴連絡とルール厳守宣言は必ず実行すること。

---

## コンテキスト制約時の対応
```

### after（新規セクション挿入後）

```markdown
## 前処理の phase-report-check (verify) 必須実行

全フェーズスキルの前処理で `phase-report-check (aide-powers skill: verify)` を activate して実行することは**省略禁止**である。verify モードには以下が含まれており、これらを毎フェーズ開始時に必ず実行しなければならない:

- 直前フェーズの完了状態確認
- 違反履歴ファイルの確認とユーザーへの連絡（違反履歴がある場合）
- ルール厳守宣言（本フェーズをルールに従って実行する宣言）

「先頭フェーズだから前フェーズ確認は不要」であっても、違反履歴連絡とルール厳守宣言は必ず実行すること。

---

## 各Step・前処理・後処理の実行前の責務宣言

全フェーズスキル（`fs-*`）は、以下の3つのタイミングで責務宣言を行うことが**省略禁止**である:

1. **前処理の実行開始前**
2. **各Step の実行開始前**
3. **後処理の実行開始前**

各タイミングで、実行を開始する前に以下の宣言を行うこと:

> 「私の責務はskill記述を正確に実施することです。私は勝手な自己解釈で作業しません、skillの記述に従ってひとつづつ正確に作業します。」

### 省略禁止

- 責務宣言は毎回の実行前に**必ず**行うこと。累積的に省略してはならない
- 「前のStepで宣言したから不要」「覚えているから不要」「同じフェーズ内だから不要」等の理由での省略を一切認めない
- 本ルールは phase-report-check (verify) 内の「ルール厳守宣言」（フェーズ開始時1回）とは別の独立した仕組みであり、両方を実行する必要がある

---

## コンテキスト制約時の対応
```

### 設計根拠
- approach.md §1: 既存「前処理の phase-report-check (verify) 必須実行」の直後、「コンテキスト制約時の対応」の直前に配置（フェーズ実行フロー順）
- approach.md §2: REQ-C-001 の責務宣言文言を引用形式で正確に記載
- approach.md §3: 3箇所のタイミングを番号付きリストで明記（AC-003 充足）
- approach.md §4: 「省略禁止」サブセクションで累積省略禁止・具体的な禁止理由を列挙（AC-004, AC-005 充足）
- approach.md §5: phase-skill-rules.md のみへの追加（横断ルール一元管理、AC-006, AC-007 充足）
- 既存セクションの命名パターン準拠: `## ` + 日本語名称（既存セクション「前処理の phase-report-check (verify) 必須実行」「コンテキスト制約時の対応」等と同じレベル・スタイル）
- 既存構造準拠: `---` 区切り線で前後セクションと分離（既存パターンと同一）

---

## 2. 既存変更: version.json — バージョンインクリメント

### 対象ファイル
`skills/using-aide-powers/references/version.json`

### 変更理由
phase-skill-rules.md の正本更新に伴い、rules-distribute スキルによる配布トリガーとして version を更新する必要がある。version が更新されることで、using-aide-powers の起動時手順（STEP 2）で `.aide/references/` へのコピーが実行され、配布先（`.kiro/steering/aide-powers-phase-skill-rules.md` 等）が自動更新される。

### before

```json
{
  "version": 14,
  "updated": "2026-06-25"
}
```

### after

```json
{
  "version": 15,
  "updated": "2026-06-25"
}
```

### 設計根拠
- approach.md §6: version を 14→15 に更新（配布トリガー）
- updated は同日のため変更なし（実装時の日付に合わせて調整可）

---

## 受入基準とのトレーサビリティ

| 受入基準 | 充足箇所 |
|---|---|
| AC-001: phase-skill-rules.md にルール追加 | §1 新規セクション追加 |
| AC-002: 責務宣言文言が正確に定義 | §1 after 内の引用ブロック |
| AC-003: 3箇所のタイミング明記 | §1 after 内の番号付きリスト（前処理・各Step・後処理） |
| AC-004: 省略禁止の明記 | §1 after 内「省略禁止」サブセクション |
| AC-005: 毎回実行の明記 | §1 after 内「毎回の実行前に**必ず**行うこと」 |
| AC-006: phase-skill-rules.md に追加 | §1 対象ファイル |
| AC-007: 横断ルールとして定義 | §1 設計根拠（個別SKILL.mdへの埋め込みなし） |

---

## インターフェース影響サマリ

本変更は新規セクション追加のみであり、既存セクションのシグネチャ・構造に変更はない。

| 影響項目 | 影響有無 | 備考 |
|---|---|---|
| 既存セクションの変更 | なし | 追加のみ |
| 他ファイルのシグネチャ変更 | なし | phase-skill-rules.md はルール文書であり、API/シグネチャを持たない |
| 配布先の自動更新 | あり | version.json 更新により rules-distribute 経由で自動配布される |

---

## 更新が必要な設計資料

| 設計資料 | 更新内容 | 備考 |
|---|---|---|
| なし | — | 本変更はルール文書への追加であり、object-design/gui-design/infra-interface-design 等の設計書は存在しない（dev-environment.md §14参照）。program-structure.md のセクション一覧への影響もなし（phase-skill-rules.md のセクション構成はprogram-structure.mdに詳細列挙されていない） |
