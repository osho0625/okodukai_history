# 影響範囲分析（更新版）

> Phase 1 版の影響分析を差分設計（QA APPROVED済み）を踏まえて再調査・更新したもの

## 変更種別
追加（Markdownルール文書へのセクション追加 + JSONの値変更のみ）

---

## 1. アクター視点の影響

### 影響を受けるユースケース

| ユースケースID | 要件 | 影響内容 |
|---|---|---|
| UR-001 | 7つのワークフローを提供すること | 全WF（7種）のフェーズスキル（約40ファイル）に横断的に適用されるルール追加。SKILL.md自体の変更は不要（横断ルールとして自動適用） |
| UR-015 | ルール配布機構（rules-distribute）によるAIコンテキストへの自動ルール注入 | version.json の 14→15 更新をトリガーに、既存配布メカニズムで自動対応 |

### 影響を受けるアクター

| アクター | 影響の種類 | 影響内容 | 直接/間接 |
|---|---|---|---|
| AIエージェント（全フェーズスキル実行主体） | 動作変更 | 各Step・前処理・後処理の実行前に責務宣言が必須化される。ルール参照タイミングが増加する | 直接 |
| AIエージェントを使うソフトウェア開発者 | 観察可能な変更 | AIの出力に責務宣言が追加表示される。操作変更なし | 間接 |

### 説明対象アクター

| アクター | 説明の要否 | 理由 |
|---|---|---|
| AIエージェント | 不要（自動適用） | phase-skill-rules.md は全フェーズスキルの前処理で自動読み込みされるため、追加セクションは次回実行時から自動適用される |
| ソフトウェア開発者 | 不要 | ユーザーの操作に変更なし。AIの出力に宣言が追加されるが、ユーザーへの説明や操作変更の案内は不要 |

---

## 2. プログラム構成視点の影響

### 変更対象ファイル

| ファイル | 変更種別 | 変更概要 |
|---|---|---|
| `skills/using-aide-powers/references/phase-skill-rules.md` | セクション追加 | 「各Step・前処理・後処理の実行前の責務宣言」セクションの新規追加（「前処理の phase-report-check (verify) 必須実行」の直後） |
| `skills/using-aide-powers/references/version.json` | 値変更 | version を 14→15 に更新（配布トリガー） |

### シグネチャ変更の全件追跡結果

| 確認項目 | 結果 |
|---|---|
| プログラムコード（.py/.js/.ts等）のシグネチャ変更 | なし（本プロジェクトにプログラムコードは存在しない） |
| 関数・クラス・API変更 | なし |
| 既存セクションの構造変更 | なし（before→afterで既存セクションは無変更） |
| 他ファイルへの変更波及 | なし（追加のみで参照構造は不変） |

### 依存関係分析（変更対象を参照しているファイル）

| ファイル | 依存内容 | 影響レベル | 変更要否 |
|---|---|---|---|
| `skills/using-aide-powers/SKILL.md` | phase-skill-rules.md を references として管理・配布指示 | 低 | 不要（ルール追加のみで参照構造は不変） |
| `skills/rules-distribute/SKILL.md` | phase-skill-rules.md を配布対象として定義 | 低 | 不要（既存の配布フローで自動対応） |
| `skills/step-history-writer/SKILL.md` | phase-skill-rules.md の原則を参照 | 低 | 不要（ルール追加のみで既存参照に影響なし） |
| 全フェーズスキル `skills/fs-*/SKILL.md`（約40ファイル） | 前処理で phase-skill-rules.md を読み込み重要ポイントを記載 | 中 | 不要（新ルール追加により AIの動作に宣言ステップが追加されるが、SKILL.md自体の変更は不要） |
| `.kiro/steering/aide-powers-phase-skill-rules.md` | 配布先コピー（rules-distribute経由で自動更新） | 低 | 不要（自動配布で対応） |
| `.apm/instructions/aide-powers-phase-skill-rules.instructions.md` | APM配布用コピー | 中 | **要同期更新**（rules-distribute SKILL.md §注記に記載の配布ファイル同期） |

### 配布ファイル同期対象（rules-distribute §注記 6本）

正本更新後に同期更新が必要なファイル:

| # | ファイル | プラットフォーム |
|---|---|---|
| 1 | `steering/aide-powers-phase-skill-rules.md` | Kiro IDE / CLI |
| 2 | `rules/aide-powers-phase-skill-rules.md` | Claude Code |
| 3 | `instructions/aide-powers-phase-skill-rules.instructions.md` | GitHub Copilot |
| 4 | `.apm/instructions/aide-powers-phase-skill-rules.instructions.md` | APM |
| 5 | `.kiro/steering/aide-powers-phase-skill-rules.md` | ワークスペースコピー |
| 6 | （上記以外の Cursor / OpenCode / Gemini / Codex 用） | 配布時生成 |

> **注**: これらは rules-distribute スキルの既存フローで自動対応される。version.json 更新 → using-aide-powers 起動時手順 STEP 2 で `.aide/references/` 置き換え → STEP 3 で rules-distribute が配布先を自動更新。手動対応が必要なのは APM配布ファイル（リポジトリ内の `steering/`, `rules/`, `instructions/` 配下）のみ。

---

## 3. 既存要件・システム要件との矛盾確認

### user-requirements.md との整合性

| 確認観点 | 結果 |
|---|---|
| UR-001（7つのWF提供） | 矛盾なし。全WFに横断適用されるルール追加であり、WF提供能力を損なわない |
| UR-015（ルール配布機構） | 矛盾なし。既存の配布メカニズム（version.json→using-aide-powers STEP2→rules-distribute）をそのまま活用 |
| UR-010（共通スキル群） | 矛盾なし。共通スキル（`fs-*` 以外）は対象外と明記されている |
| その他全要件 | 矛盾なし。本変更はフェーズスキル実行時の動作ルール追加のみであり、既存機能を削除・変更しない |

### system-requirements.md との整合性

| 確認観点 | 結果 |
|---|---|
| §4.1 エラーハンドリング体系 | 影響なし。責務宣言はエラー分類に影響しない |
| §4.2 エラー伝播ルール | 影響なし |
| §7.1 マルチプラットフォーム対応 | 影響なし。ルール文書の追加は全プラットフォームで同一に配布される |
| §7.5 文字エンコーディング | 影響なし。UTF-8 LF で追記 |
| §7.6 ファイル書き込み制約 | 影響なし。追加セクションは50行未満 |
| NF-13 自動テストなし方針 | 影響なし。本変更に自動テストは不要 |

---

## 4. テスト対象機能の特定

### 自動テスト対象

なし。本変更はMarkdownルール文書へのセクション追加であり、自動テスト可能な「機能」（関数・API・コマンド等）は存在しない。system-requirements.md §7.4 NF-13（自動テストフレームワーク非導入）の方針とも整合。

### 手動テスト対象

| # | テスト対象 | テスト方法 | 確認観点 |
|---|---|---|---|
| 1 | 配布メカニズムの動作 | version.json 更新後に using-aide-powers を起動し、`.aide/references/` が更新されること | version 比較→コピー→フラグ生成→配布が正常に動作するか |
| 2 | 責務宣言の実行 | 任意のフェーズスキルを実行し、各Step開始前に責務宣言が出力されること | ルール追加が AI の動作に反映されているか |
| 3 | 既存動作への非影響 | フェーズスキルの前処理（phase-report-check verify）が従来通り動作すること | 既存セクション無変更のため回帰なし |

---

## 5. リスク評価

| リスク項目 | レベル | 理由 | 対策 |
|---|---|---|---|
| 既存機能への回帰 | 極低 | 既存セクション無変更。新規セクション追加のみ | before→after で既存部分が完全一致であることを実装時に確認 |
| AIの実行時間増加 | 低 | 各Step前に宣言テキスト出力が追加される | 宣言は短い固定文言のみ（1行）。処理時間への影響は無視できる |
| APM配布ファイルの同期漏れ | 中 | 正本更新とAPM配布ファイルの同期が別作業 | 実装タスクにAPM配布ファイル同期を含めること |

---

## 6. 起因元ドキュメントフォルダ

- パス: なし
- コミットハッシュ: なし
- 検証結果: 本変更は新規ルール追加（PI-046起票）であり、既存変更フォルダとの統合対象なし

---

## 更新履歴

| 版 | 日時 | 内容 |
|---|---|---|
| Phase 1 | 初回作成 | 軽量版の影響分析 |
| 更新版 | 差分設計QA APPROVED後 | delta-design.md の内容を踏まえた再調査。テスト対象機能・説明対象アクターの特定。既存要件との矛盾確認追加。リスク評価追加 |
