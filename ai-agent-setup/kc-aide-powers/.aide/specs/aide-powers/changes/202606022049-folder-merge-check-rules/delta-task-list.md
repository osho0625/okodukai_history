# 差分タスクリスト

## メタ情報

| 項目 | 値 |
|---|---|
| feature_name | aide-powers |
| changes_dir | .aide/specs/aide-powers/changes/202606022049-folder-merge-check-rules |
| 変更種別 | 変更（既存スキル定義の拡張） |
| 成果物種別 | 非プログラム成果物（スキル定義テキストファイル） |
| 自動テスト | なし（dev-environment.md §7.4 確定済み） |

---

## タスク一覧

### 1. skills/folder-merge-check/SKILL.md への退避ルール追記

| 項目 | 内容 |
|---|---|
| 対象ファイル | `skills/folder-merge-check/SKILL.md` |
| テストファイル | ➖ skip（非プログラム成果物） |
| 依存タスク | なし（単一タスク） |
| 変更内容 | delta-design.md の変更対象1・変更対象2を反映する |

#### 変更詳細

**変更対象1: Step 4 への事前退避サブステップ挿入**

- Step 4 セクションの既存「移動ルール」の**前**に「Step 4-事前: 統合先の前WF (b)分類成果物の一括退避」サブステップを挿入する
- delta-design.md「変更対象1 → after」の内容を正確に反映する
- 挿入内容:
  1. 退避対象の検出（統合先の (b)分類ファイル一括検出）
  2. 退避先の日付決定（優先1: history.md → 優先2: 進捗ファイル日時 → 優先3: 本日日付）
  3. 進捗ファイルの退避判定（全フェーズ完了→無条件退避 / 未完了→ユーザー確認）
  4. 退避の実行（old/{日付}/ への移動）
  5. 退避完了後の確認

**変更対象2: 完了条件への退避処理条件追記**

- 完了条件「統合した場合」セクションの先頭に条件1・2を追加する
- 既存の条件1〜5を条件3〜7にリナンバリングする
- delta-design.md「変更対象2 → after」の内容を正確に反映する

#### 設計参照

- delta-design.md: 変更対象1（before/after）、変更対象2（before/after）
- approach.md: §変更方針 → 具体的な変更方針

#### 完了基準

- [ ] Step 4 セクションに「Step 4-事前」サブステップが挿入されている
- [ ] 既存の移動ルールは「Step 4-事前」の後に配置されている（処理順序: 退避→移動）
- [ ] 完了条件に条件1・2が追加され、既存条件が3〜7にリナンバリングされている
- [ ] delta-design.md の before/after と diff を取った場合、after と一致する

---

## 依存関係グラフ

```
タスク1（単一タスク・依存なし）
```

## 実装順序

| 順序 | タスク | 理由 |
|---|---|---|
| 1 | タスク1 | 単一ファイルへの変更。依存関係なし |
