# ユースケース一覧: aide-powers

## 概要
- 抽出元: user-requirements.md, system-requirements.md, system-architecture.md
- 利用者: 一般ユーザー（社内メンバー）、プラグイン管理者（ビルド・配布担当者）
- UC総数: 50件

## 一般ユーザー（社内メンバー）のユースケース

### 企画オーケストレーターの利用シナリオ

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-001 | 漠然としたアイデアから開発企画書を作り上げる | 「こんなものを作りたい」という漠然とした構想を自然言語で伝え、対話を通じて開発企画書として具体化する | UR(REQ-M01) | Must |
| UC-002 | 技術的な実現可能性を調査してもらう | アイデアに対して、どの技術が使えるか・制約はあるかを調査してもらい、企画の判断材料を得る | SA(企画Phase2) | Must |
| UC-003 | ユーザー提供資料をもとに企画を整理してもらう | 既存の資料（メモ、参考URL、スクリーンショット等）を渡して、企画書の素材として構造化してもらう | SA(企画Phase0) | Should |
| UC-004 | 企画書のレビューを受けて品質を高める | 作成中の企画書に対してレビューを受け、抜け漏れや矛盾を指摘してもらい改善する | SA(企画Phase2) | Must |

### 設計オーケストレーターの利用シナリオ

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-005 | 企画書をもとに要件定義から設計完了まで一貫して進める | 企画書（または明確な要件）を入力として、ユーザー要件定義→システム要件定義→開発計画→システム構成→GUI設計→ユースケース分析→DDD→オブジェクト設計→インフラIF→プログラム構成→QAゲートまでを一貫して完了する | UR(REQ-M01) | Must |
| UC-006 | 対話を通じてユーザー要件を明確にする | 自分のやりたいことを自然言語で伝え、サブエージェントとの対話を通じてユーザー要件定義書として整理してもらう | SA(設計Phase1) | Must |
| UC-007 | 設計品質をDDD・SOLIDの観点で保証してもらう | 設計フェーズ完了時にQAゲートを通過させ、DDD・SOLID等の設計原則に準拠した品質の高い設計書を得る | UR(REQ-M09) | Must |
| UC-008 | 要件が明確な場合に企画フェーズを経ずに設計から始める | 何を作るかが明確な場合、企画オーケストレーターを経由せず直接設計オーケストレーターから開始する | SA(選択ガイド) | Must |

### 実装オーケストレーターの利用シナリオ

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-009 | 設計書に基づいてコードを自動実装してもらう | 完成した設計書一式を入力として、タスク分解→実装→レビューまでを自動で進めてもらい、動作するコードを得る | UR(REQ-M01) | Must |
| UC-010 | 実装の進捗をタスク単位で把握する | 実装タスクリストと進捗管理により、どこまで完了しているか・何が残っているかを確認する | SA(実装) | Should |
| UC-011 | 実装完了後にドキュメント（README等）を自動生成してもらう | 実装完了後、プロジェクトのREADMEやdocs/配下のドキュメントを自動生成してもらう | SA(実装Phase3) | Should |

### 設計逆引きオーケストレーターの利用シナリオ

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-012 | 既存コードから設計書を逆生成する | 設計書がない既存プロジェクトに対して、コードを解析して設計書一式（プログラム構成→開発環境→システム要件→ユーザー要件）を逆生成してもらう | UR(REQ-M01) | Must |
| UC-013 | 逆生成した設計書をもとに変更・追加作業を可能にする | 設計書がなかったプロジェクトに設計書を整備し、変更オーケストレーターや実装オーケストレーターを利用できる状態にする | SA(逆引き→変更/実装) | Must |
| UC-014 | 既存コードのDDD・レイヤード構造を可視化する | 既存コードにレイヤー構造やDDDパターンがある場合、それを設計書として可視化してもらう | SA(逆引きオプション) | Could |

### 変更オーケストレーターの利用シナリオ

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-015 | 既存機能に新しい機能を追加する | 既存プロジェクトに対して「〇〇機能を追加したい」とリクエストし、影響分析→差分設計→差分実装→設計書反映まで一貫して進めてもらう | UR(REQ-M01) | Must |
| UC-016 | 既存機能の仕様を変更する | 既存機能の振る舞いを変更したい場合に、変更要求定義→影響範囲分析→対応方針→差分設計→実装→設計書反映まで進めてもらう | UR(REQ-M01) | Must |
| UC-017 | 変更の影響範囲を事前に把握する | 変更を実施する前に、どのファイル・どの機能に影響があるかを分析してもらい、リスクを把握する | SA(変更Phase2) | Must |
| UC-018 | 変更に伴うリファクタリングの必要性を判断してもらう | 変更の対応方針策定時に、リファクタリングが必要かどうかを判断してもらい、必要な場合はリファクタリングオーケストレーターへの引き継ぎ書を作成してもらう | SA(変更Phase3) | Should |

### バグ修正オーケストレーターの利用シナリオ

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-019 | バグの症状を伝えて原因を特定・修正してもらう | 「〇〇が動かない」「〇〇でエラーが出る」等の症状を自然言語で伝え、原因分析→修正方針→差分設計→実装→ドキュメント反映まで進めてもらう | UR(REQ-M01) | Must |
| UC-020 | 設計書がないプロジェクトでもバグ修正を進める | 設計書がなくても、コードから直接原因を分析してバグ修正を進めてもらう | SA(バグ修正) | Must |
| UC-021 | バグ修正後にリグレッションテストで再発を防止する | バグ修正時にリグレッションテストを設計・実行してもらい、修正が他の機能に影響していないことを確認する | SA(バグ修正Phase4-5) | Must |

### リファクタリングオーケストレーターの利用シナリオ

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-022 | 外部振る舞いを変えずに内部構造を改善する | コードの可読性・保守性を向上させるために、外部振る舞いを保持したまま内部構造を改善してもらう | UR(REQ-M01) | Must |
| UC-023 | リファクタリング候補を提案してもらう | 現状のコードを分析して、リファクタリングすべき箇所を効果・コスト・リスクの観点で一覧化してもらう | SA(リファクタリングPhase1) | Must |
| UC-024 | 変更オーケストレーターからの引き継ぎでリファクタリングを実施する | 変更オーケストレーターが作成したrefactoring-request.mdをもとに、リファクタリングを実施する | SA(変更Phase3→リファクタリング) | Should |

### オーケストレーター自動選択に関するユースケース

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-025 | オーケストレーターを意識せず自然言語でリクエストする | 「〇〇を作りたい」「〇〇を直して」等の自然言語リクエストだけで、適切なオーケストレーターが自動選択されて作業が始まる | UR(REQ-M08) | Must |
| UC-026 | 自動選択されたオーケストレーターが意図と異なる場合に変更する | 自動選択の結果が意図と異なる場合に、正しいオーケストレーターに切り替えてもらう | 想定 | Should |
| UC-027 | 未対応の問題（pending-issues）を優先的に案内してもらう | セッション開始時にpending-issues.mdの内容を確認し、未対応の問題がある場合に優先対応を案内してもらう | SA(選択ガイド) | Should |

### スキルチェーン間遷移に関するユースケース

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-028 | 企画から設計、設計から実装へとシームレスに開発を進める | 企画完了→設計開始→設計完了→実装開始の流れを、引き継ぎファイル（handover-notes.md、設計書一式）を介してスムーズに遷移する | UR(REQ-M01), SA(CON-05) | Must |
| UC-029 | 設計書がない状態で実装を依頼した場合に逆引きを案内してもらう | 設計書なしで実装オーケストレーターを起動しようとした場合、設計書ゲートで停止し、設計逆引きオーケストレーターの実行を案内してもらう | SA(実装HARD-GATE) | Must |
| UC-030 | 変更・リファクタリング作業中に設計書未完了を検知して逆引きを案内してもらう | 変更オーケストレーターのフェーズ0またはリファクタリングオーケストレーターの設計書ゲートで設計書が未完了と判定された場合、設計逆引きオーケストレーターの実行を案内してもらう | SA(変更Phase0, リファクタリング前提条件) | Must |

### エラー時のフォールバックに関するユースケース

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-031 | サブエージェントが情報不足の場合に直接質問を受ける | サブエージェントがNEEDS_CONTEXTを報告した場合、サブエージェントから直接質問を受けて追加情報を提供する | UR(REQ-M05), SR(§4.1) | Must |
| UC-032 | サブエージェントが完了不能な場合に段階的な対応を受ける | サブエージェントがBLOCKEDを報告した場合、コンテキスト追加→タスク分割→ユーザーエスカレーション（モデル切り替え提案を含む）の段階的対応を受ける | SR(§4.1) | Must |
| UC-033 | QAゲート不通過時に自動修正を経て品質を確保する | QAゲートでREJECTEDとなった場合、指摘事項に基づいて担当エージェントが修正を行い、再QAを経て品質が確保される（APPROVED まで繰り返し） | SR(§4.4) | Must |
| UC-034 | 3回修正失敗時にアーキテクチャ問題としてエスカレーションを受ける | デバッグで3回修正に失敗した場合、アーキテクチャの問題と判断され、ユーザーにエスカレーションされて方針を相談できる | SR(§4.5) | Must |
| UC-035 | 懸念付き完了（DONE_WITH_CONCERNS）の内容を確認して判断する | サブエージェントが懸念付きで完了した場合、懸念の内容を確認し、対処するかそのまま進めるかを判断する | SR(§4.1) | Should |

### マルチプラットフォーム利用・インストールに関するユースケース

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-036 | 好みのエディタ/CLIでAIDEの開発プロセスを利用する | Claude Code、Kiro、Cursor、VSCode Copilot、Codex CLI、Gemini CLI、Copilot CLI、OpenCodeのいずれかの環境で、同じAIDEの開発プロセスを利用する | UR(REQ-M02) | Must |
| UC-037 | タスクトレイアプリからaide-powersをインストール・利用開始する | タスクトレイ常駐アプリ（配布されたexe）を実行するだけでaide-powersが利用可能になり、10分以内に初回実行できる | UR(REQ-M07) | Must |

### プロジェクト管理・開発ツール起動に関するユースケース

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-043 | プロジェクトを登録して開発ツールで開く | タスクトレイアプリにプロジェクトフォルダを登録し、環境変数（証明書等）が設定された状態で開発ツール（Kiro IDE、VSCode等）を起動してワークスペースとして開く。現在のkirop.bat相当の操作をGUIで行う | UR(REQ-M16) | Must |
| UC-044 | 開発ツール起動時の環境変数をカスタマイズする | 開発ツール起動時に設定される環境変数（証明書パス、UV設定、MCP設定等）をGUIで追加・編集・削除する。デフォルトのプリセットから変更したい場合に使用する | UR(REQ-M16) | Should |
| UC-045 | 初期設定で証明書を設定して社内ネットワーク対応を完了する | 初期設定ウィザードで社内プロキシ対応のCA証明書ファイルパスを設定し、開発ツールやストレージ接続が社内ネットワーク経由で正常動作する状態にする | UR(REQ-M18) | Must |

### 品質保証・レビューに関するユースケース

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-038 | 実装結果を設計書準拠とコード品質の2段階でレビューしてもらう | 実装完了後、まず設計書との整合性チェック、次にコード内部品質チェックの2段階レビューを受け、品質を確保する | UR(REQ-M04), SR(§4.4) | Must |
| UC-039 | 複数の観点（品質・アーキテクチャ・テスト・セキュリティ等）で並列レビューを受ける | コードレビュー時に、コード品質・アーキテクチャ・テスト・要件準拠・パフォーマンス・セキュリティ等の複数観点で並列にレビューを受ける | UR(REQ-S03) | Should |

### その他の一般ユーザーユースケース

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-040 | サブエージェントと直接対話して要件や設計の詳細を詰める | オーケストレーターを経由せず、サブエージェントと直接対話して質問に答えたり確認を行ったりする | UR(REQ-M05) | Must |
| UC-041 | 開発プロセスの各フェーズで生成された成果物を確認・承認する | 各フェーズ完了時に生成された成果物（設計書、コード等）を確認し、次のフェーズに進むかどうかを判断する | UR(REQ-M01) | Must |
| UC-042 | 再構成の設計判断・手順を記録として参照する | aide-powersの再構成工程がドキュメント化されており、将来の更新・拡張時に設計判断の経緯を参照できる | UR(REQ-S02) | Should |

---

## プラグイン管理者（ビルド・配布担当者）のユースケース

### ビルド・パッケージングに関するユースケース

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-101 | タスクトレイアプリ（tkinter）をビルドしてexeを生成する | aide-powersのタスクトレイ管理アプリをビルドし、配布可能なexeファイルを生成する | UR(REQ-M07) | Must |
| UC-102 | aide-powersのコードをパッケージングする | aide-powersのスキル・エージェント定義ファイル一式を配布用パッケージとしてまとめる | UR(REQ-M07) | Must |

### 配布・公開に関するユースケース

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-103 | MinIO/S3にexeとaide-powersコードを配置して社内公開する | ビルドしたexeとパッケージングしたaide-powersコードをMinIO（またはS3）にアップロードし、社内メンバーがダウンロードできる状態にする | UR(REQ-M06), SR(§6.1) | Must |
| UC-104 | バージョン情報を更新してリリースする | version.txt等のバージョン管理ファイルを更新し、タスクトレイアプリが新バージョンを検知できるようにする | SR(§7.4) | Must |

### アクセス管理に関するユースケース

| UC-ID | ユースケース名 | 概要 | 抽出元 | 優先度 |
|-------|---------------|------|--------|--------|
| UC-105 | 社内メンバーのみがアクセスできるよう配布先を管理する | MinIO/S3のアクセス権限を設定し、社内メンバーのみがaide-powersをダウンロードできる状態を維持する | UR(REQ-M06) | Must |

---

## 網羅性チェック

### user-requirements.md の全項目がUCとして表現されているか: OK

| 要件ID | 対応UC |
|--------|--------|
| REQ-M01（既存AIDEと同等以上の成果物生成） | UC-001〜UC-024（7オーケストレーター全カバー）, UC-028, UC-041 |
| REQ-M02（マルチプラットフォーム対応） | UC-036, UC-037 |
| REQ-M03（superpowers形式への最適化再構成） | UC-005, UC-009（内部プロセスとして反映。利用者の直接目的ではない） |
| REQ-M04（superpowersの仕組みの取り込み） | UC-031〜UC-035, UC-038 |
| REQ-M05（サブエージェントのユーザー対話維持） | UC-031, UC-040 |
| REQ-M06（社内限定配布） | UC-103, UC-105 |
| REQ-M07（容易なインストールと初回実行） | UC-037, UC-101 |
| REQ-M08（オーケストレーター自動選択） | UC-025, UC-026, UC-027 |
| REQ-M09（設計品質保証の維持・強化） | UC-007, UC-033, UC-038 |
| REQ-M10（kiro専用AIDEの開発停止と移行） | UC-036（全プラットフォーム共通利用として反映） |
| REQ-M11（オーケストレーターの実作業禁止の維持） | 内部制約であり利用者のUCではない（品質保証として UC-033, UC-038 に間接反映） |
| REQ-M16（プロジェクトルート管理と開発ツールランチャー） | UC-043, UC-044 |
| REQ-M17（グローバル環境の非汚染） | 内部制約であり利用者のUCではない（開発環境の設計方針として反映） |
| REQ-M18（セットアップ時の証明書設定） | UC-045 |
| REQ-S01（コンテキスト効率の向上） | 内部品質であり利用者のUCではない |
| REQ-S02（再構成工程のドキュメント化） | UC-042 |
| REQ-S03（複数観点の並列レビュー） | UC-039 |
| REQ-S04（将来のdesk-agents追加への拡張性） | 内部設計方針であり利用者のUCではない |

### 要件から想定可能な追加UCがあるか: 追加済み

以下のUCはユーザー要件に明記されていないが、要件から想定可能なものとして追加した:
- UC-003（ユーザー提供資料の構造化）: 企画フェーズの実際の利用シーンとして想定
- UC-010（実装進捗の把握）: 実装タスクリスト・進捗管理の利用シーンとして想定
- UC-014（既存コードのDDD・レイヤード構造の可視化）: 逆引きオプションフェーズの利用シーンとして想定
- UC-026（オーケストレーター変更）: 自動選択の誤判定時の利用シーンとして想定
- UC-035（懸念付き完了の確認）: 4ステータス管理のDONE_WITH_CONCERNSの利用シーンとして想定

### 機能定義・操作手順がUCに混入していないか: OK

全UCが「利用者の目的（やりたいこと）」として記述されていることを確認済み。
「〜画面を表示する」「〜ボタンを押す」「〜コマンドを実行する」等の機能定義・操作手順は含まれていない。
