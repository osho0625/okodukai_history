{
  "name": "okodukai-history",
  "version": "1.79.3",
  "private": true,
  "scripts": {
    "test": "vitest --run",
    "generate-vapid": "web-push generate-vapid-keys --json"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "fast-check": "^3.22.0",
    "vitest": "^3.1.3"
  }
}
