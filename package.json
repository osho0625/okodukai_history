{
  "name": "okodukai-history",
  "version": "2.25.1",
  "private": true,
  "scripts": {
    "test": "vitest --run",
    "generate-vapid": "web-push generate-vapid-keys --json"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "fast-check": "^3.22.0",
    "jsdom": "^29.1.1",
    "vitest": "^3.1.3"
  }
}
